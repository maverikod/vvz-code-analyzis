# Analysis: CST Tool Simplification Proposal

**Author**: Vasiliy Zdanovskiy  
**Email**: vasilyvz@gmail.com  
**Date**: 2025-01-12  
**Topic**: Упрощение использования CST инструментов

---

## 🔍 Current Complexity Analysis

### 1. **Множественные шаги для простой задачи**

**Текущий workflow**:
```
1. list_cst_blocks → получить block_id
2. query_cst → получить node_id (если нужно)
3. compose_cst_module → сформировать сложный JSON с операциями
4. Повторить для каждой операции
```

**Проблема**: Для простого изменения нужно выполнить несколько команд и правильно сформировать JSON.

### 2. **Сложная схема селекторов**

**Текущие типы селекторов**:
- `function` - по имени функции
- `class` - по имени класса
- `method` - по квалифицированному имени "ClassName.method"
- `range` - по строкам (start_line, end_line)
- `block_id` - по стабильному ID из list_cst_blocks
- `node_id` - по ID из query_cst
- `cst_query` - по CSTQuery селектору
- `module` - для создания нового модуля

**Проблема**: 
- Нужно знать, какой селектор использовать в каждом случае
- Разные селекторы требуют разных параметров
- Сложная валидация и обработка ошибок

### 3. **Сложная структура операций**

**Текущая структура**:
```json
{
  "ops": [{
    "operation_type": "replace|insert|create",
    "selector": {
      "kind": "...",
      "name": "...",
      "start_line": ...,
      "end_line": ...,
      // и т.д.
    },
    "new_code": "...",
    "position": "before|after|end|...",
    "file_docstring": "..." // для module
  }]
}
```

**Проблема**:
- Много обязательных/опциональных полей
- Сложная валидация (allOf, if/then)
- Разные поля для разных типов операций
- Легко ошибиться в структуре

### 4. **Промежуточные команды**

**Текущий подход**:
- Нужно сначала вызвать `list_cst_blocks` или `query_cst`
- Получить ID
- Использовать ID в `compose_cst_module`

**Проблема**: 
- Дополнительные шаги
- Нужно запоминать/передавать ID между командами
- Увеличивает сложность workflow

---

## 💡 Proposed Simplification

### Предложение: Императивный подход с CST деревом

```
1. Задаешь файл
2. Инструмент читает его в CST дерево
3. Работаешь с узлами CST (добавляешь, удаляешь, редактируешь)
4. Записываешь на диск
```

### Преимущества:

1. **Прямая работа с узлами**
   - Не нужно знать типы селекторов
   - Работаешь с конкретными узлами дерева
   - Более интуитивно

2. **Меньше промежуточных шагов**
   - Одна команда вместо нескольких
   - Не нужно получать ID заранее
   - Проще workflow

3. **Более гибкий**
   - Можешь работать с любыми узлами
   - Не ограничен типами селекторов
   - Можешь делать множественные изменения за раз

---

## 🎯 Proposed API Design

### Вариант 1: Session-based (интерактивный)

```python
# 1. Открыть файл и получить CST дерево
session = cst_open_file("path/to/file.py")
tree = session.get_tree()

# 2. Найти узлы (простой поиск)
function_node = tree.find_function("main")
class_node = tree.find_class("MyClass")

# 3. Работать с узлами
function_node.replace_body(new_code)
class_node.add_method(method_code)
tree.remove_node(some_node)

# 4. Сохранить
session.save()
```

**Плюсы**:
- Интуитивно понятно
- Прямая работа с узлами
- Можно делать множественные изменения

**Минусы**:
- Требует stateful сессию
- Сложнее для MCP (stateless протокол)

### Вариант 2: Batch operations (декларативный, но упрощенный)

```json
{
  "file_path": "path/to/file.py",
  "operations": [
    {
      "action": "replace",
      "target": {
        "type": "function",
        "name": "main"
      },
      "code": "def main():\n    pass"
    },
    {
      "action": "insert",
      "target": {
        "type": "class",
        "name": "MyClass"
      },
      "position": "after",
      "code": "def new_method(self):\n    pass"
    },
    {
      "action": "remove",
      "target": {
        "type": "function",
        "name": "old_function"
      }
    }
  ]
}
```

**Плюсы**:
- Stateless (подходит для MCP)
- Упрощенная структура
- Множественные операции в одном запросе

**Минусы**:
- Все еще декларативный подход
- Нужно знать структуру target

### Вариант 3: Hybrid (рекомендуемый)

**Команда 1: `cst_load_file`** - загрузить файл в CST дерево
```json
{
  "file_path": "path/to/file.py",
  "return_tree": true  // вернуть представление дерева
}
```

**Ответ**:
```json
{
  "tree_id": "uuid-123",
  "nodes": [
    {
      "id": "node-1",
      "type": "FunctionDef",
      "name": "main",
      "start_line": 10,
      "end_line": 15,
      "children": [...]
    },
    // ...
  ]
}
```

**Команда 2: `cst_modify_tree`** - модифицировать дерево
```json
{
  "tree_id": "uuid-123",
  "operations": [
    {
      "action": "replace",
      "node_id": "node-1",
      "code": "def main():\n    new_code()"
    },
    {
      "action": "insert",
      "parent_node_id": "node-2",
      "position": "after",
      "code": "def new_method(self):\n    pass"
    },
    {
      "action": "remove",
      "node_id": "node-3"
    }
  ]
}
```

**Команда 3: `cst_save_tree`** - сохранить дерево в файл
```json
{
  "tree_id": "uuid-123",
  "file_path": "path/to/file.py",
  "validate": true,
  "backup": true
}
```

**Плюсы**:
- Stateless (tree_id можно хранить в памяти клиента)
- Прямая работа с узлами через ID
- Множественные операции
- Валидация перед сохранением

**Минусы**:
- Требует 3 команды вместо 1
- Нужно управлять tree_id

---

## 🔄 Comparison: Current vs Proposed

### Текущий подход (декларативный):

**Задача**: Заменить тело функции `main`

**Шаги**:
1. `list_cst_blocks` → получить block_id для функции main
2. `compose_cst_module` с сложным JSON:
   ```json
   {
     "ops": [{
       "operation_type": "replace",
       "selector": {
         "kind": "block_id",
         "block_id": "func-main-123"
       },
       "new_code": "def main():\n    new_code()"
     }]
   }
   ```

**Проблемы**:
- 2 команды
- Сложный JSON
- Нужно знать block_id формат

### Предложенный подход (императивный):

**Задача**: Заменить тело функции `main`

**Шаги**:
1. `cst_load_file` → получить tree_id и найти node для main
2. `cst_modify_tree`:
   ```json
   {
     "tree_id": "uuid-123",
     "operations": [{
       "action": "replace",
       "node_id": "node-main-456",
       "code": "def main():\n    new_code()"
     }]
   }
   ```
3. `cst_save_tree` → сохранить изменения

**Преимущества**:
- Прямая работа с узлами
- Проще структура операций
- Множественные операции в одном запросе

---

## 🎓 Recommendations

### 1. **Добавить упрощенный API поверх текущего**

Сохранить текущий `compose_cst_module` для обратной совместимости, но добавить:
- `cst_load_file` - загрузка файла в дерево
- `cst_modify_tree` - модификация дерева по node_id
- `cst_save_tree` - сохранение дерева

### 2. **Упростить селекторы в текущем API**

Вместо множества типов селекторов, использовать единый формат:
```json
{
  "target": {
    "type": "function|class|method|node",
    "name": "...",  // для function/class
    "qualname": "...",  // для method
    "node_id": "...",  // для node
    "query": "..."  // CSTQuery для поиска
  }
}
```

### 3. **Добавить helper команды**

- `cst_find_node` - найти узел по простому критерию (имя, тип)
- `cst_get_node_info` - получить информацию о узле (children, parent, etc.)

---

## 📝 Conclusion

**Текущая сложность**:
- Множественные команды для простых задач
- Сложная структура селекторов
- Промежуточные шаги (list_cst_blocks, query_cst)

**Предложенное решение**:
- Прямая работа с CST узлами через ID
- Упрощенная структура операций
- Batch operations в одном запросе

**Рекомендация**: 
Реализовать hybrid подход (Вариант 3) как дополнительный API, сохранив текущий для обратной совместимости. Это даст:
- Упрощение для простых задач
- Гибкость для сложных задач
- Обратную совместимость

---

## 🔗 Related Documentation

- `docs/CST_TOOLS.md` - Текущая документация CST инструментов
- `docs/CST_QUERY.md` - Документация CSTQuery селекторов
- `code_analysis/core/cst_module/` - Реализация текущего подхода
