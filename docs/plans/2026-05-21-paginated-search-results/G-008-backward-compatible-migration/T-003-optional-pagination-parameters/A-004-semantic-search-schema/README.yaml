step_id: A-004
parent_tactical_step: T-003
name: semantic_search pagination schema extension module
target_file: code_analysis/commands/semantic_search_pagination_schema.py
operation: create_file
priority: 4
depends_on: []
concepts:
  - C-021
  - C-023
  - C-024
prompt: |
  Operation: create_file
  Target: code_analysis/commands/semantic_search_pagination_schema.py

  MRS (C-024 OptionalPaginationParameters):
  - May include paginated, job_id, include_job_id, block position number.
  - Must not be required for existing non-paginated callers.

  Precondition: code_analysis/commands/search_session_schema.py exports merge_pagination_schema.

  Do not modify semantic_search_mcp.py (monolith over 400 lines).

  Implement:
  - def get_semantic_search_schema_with_pagination() -> dict[str, Any]
    Import SemanticSearchMCPCommand from code_analysis.commands.semantic_search_mcp,
    call cls.get_schema(), return merge_pagination_schema(base).
  - def document_semantic_search_pagination_metadata() -> dict[str, str]
    Short descriptions stating paginated defaults false and results become JSON-safe in blocks.

  Constraints:
  - Under 80 lines.
  - Author header.
  - Schema-only; execute wiring is search_paginated_semantic.py.

verification:
  type: pytest
  target: tests/unit/test_semantic_search_pagination_schema.py
  expected: >
    Schema includes optional pagination properties; semantic_search required list unchanged;
    metadata helper documents all four pagination fields.
status: ready_for_review
