#!/bin/bash
# debian/preinst — stop services before upgrade/remove.

set -e

for svc in casmgr-server casmgr-postgres code-analysis-server; do
    if systemctl is-active --quiet "$svc" 2>/dev/null; then
        echo "Stopping ${svc}..."
        systemctl stop "$svc" || true
    fi
done

exit 0
