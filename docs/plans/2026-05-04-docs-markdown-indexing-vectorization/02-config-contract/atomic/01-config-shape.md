# Atomic step: config shape

Parent step: 02-config-contract

Source file or area:
code_analysis server config contract

Goal:
Define the `code_analysis.docs_indexing` object shape with `enabled`, `vectorize`, `roots`, `include`, and `exclude`.

Output:
docs/plans/2026-05-04-docs-markdown-indexing-vectorization/02-config-contract/config-contract.md

Rules:
Keep settings in main config. Do not use projectid for this feature.
