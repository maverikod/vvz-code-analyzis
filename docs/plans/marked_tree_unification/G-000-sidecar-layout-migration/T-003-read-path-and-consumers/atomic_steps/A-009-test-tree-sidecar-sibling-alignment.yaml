step_id: A-009
parent_tactical_step: T-003
name: Align test_tree_sidecar.py with sibling convention and drop pending-mechanism tests
target_file: tests/test_tree_sidecar.py
operation: modify_file
priority: 9
depends_on:
- A-001
- A-002
- A-003
- A-004
- A-005
- A-006
- A-007
- A-008
concepts:
- C-023
- C-003
status: ready_for_review
verification:
  expected: pytest collects and passes the module; it imports sibling_tree_path and no longer imports sidecar_path_for_py, pending_sidecar_path_for_py, promote_pending_sidecar_to_final, modify_tree, TreeOperation, or TreeOperationType; exactly four test functions remain (test_sidecar_path_next_to_py, test_verify_sidecar_sha, test_write_read_roundtrip, test_render_parse_roundtrip_dict); test_sidecar_path_next_to_py asserts the sibling path <source>.py.tree; grep for 'pending', 'promote_pending_sidecar_to_final', 'sidecar_path_for_py', and '.cst' in this file returns nothing.
  target: tests/test_tree_sidecar.py
  type: pytest
prompt: "Operation: modify_file\nTarget: tests/test_tree_sidecar.py\n\n## Goal\nAlign this test module with the sibling storage convention (C-003) after the\npending-sidecar mechanism and the .cst/ resolver are absent from tree_sidecar.py.\ntree_sidecar.py no longer defines these names:\nsidecar_path_for_py, pending_sidecar_path_for_py, and\npromote_pending_sidecar_to_final. Importing any of them now raises ImportError,\nso this module fails to collect under pytest until updated. After this change the\nmodule imports only surviving names plus sibling_tree_path, asserts the sibling\npath, and drops the three tests that exercised the deleted pending mechanism.\n\n## MRS excerpt\n\n**C-023 SidecarLayoutMigration**\n- definition: The sidecar layout cutover (SidecarLayoutMigration, C-023) that redirects the remaining legacy tree-path call sites (not reached by consumer integration) onto the existing sibling_tree_path function and removes the three\
  \ legacy layouts (.cst/ subdirectory, .trees/ mirror directory, .tree_sidecar adjacent file). It does not create sibling_tree_path; that function already exists from the storage work. Runs after G-001 through G-005 are built and verified end-to-end; this is the final layout cutover with no subsequent global step.\n- properties:\n  - final sidecar cutover after G-001..G-005 verification; no subsequent global step; NodeIdMap (C-025) UUID4 identity in MAP section\n  - replaces three independent legacy path resolvers (not delegates of one another) with sibling_tree_path: sidecar_path_for_py (.cst/) in cst_tree/tree_sidecar.py, the standalone resolve_trees_sidecar_path module (.trees/) in core/tree_temp/sidecar_paths.py which is removed whole, and the inline .tree_sidecar branch of the format-dispatch sidecar_path_for in tree_representation.py\n  - redirects write_sidecar_atomic to write at the sibling path\n  - removes the legacy pending-sidecar mechanism entirely\
  \ (pending_sidecar_path_for_py, resolve_sidecar_write_path, promote_pending_sidecar_to_final, _cleanup_empty_pending_dir, the _pending subdirectory, and the is_file branch in write_sidecar_atomic); it is deleted, not migrated, because the edit-session lifecycle removes the pre-session no-source state\n  - redirects the reader functions in tree_representation.py to resolve the sibling path\n  - redirects the direct legacy-path consumers onto sibling_tree_path: tree_builder, tree_saver, stable_tree, grep_block_resolver (.cst/), and tree_temp_write_commit, tree_temp_open_support (.trees/), so JSON and YAML tree files become siblings like every other format\n  - ensures TreeRepresentationRef carries the sibling path after validate_or_recreate_tree_file\n  - replaces legacy path assertions in tests with sibling path assertions\n  - hard cutover: no backward-compatibility shim\n\n**C-003 TreeFile**\n- definition: The on-disk artifact holding source content with node-id markers embedded natively\
  \ per format.\n- properties:\n  - contains source content plus markers\n  - stored as a sibling file placed directly next to the source file it describes\n  - on-disk format is natively-marked content, not a separate JSON payload\n\n## Dependency (already exists from tree storage and lifecycle (sibling storage convention))\ncode_analysis/tree/sibling_convention.py provides:\n    def sibling_tree_path(source_abs: Path) -> Path\nmapping <dir>/<stem>.<ext> -> <dir>/<stem>.<ext>.tree (full filename plus \".tree\";\ne.g. mod.py -> mod.py.tree, in the SAME directory, with no .cst/ subdirectory).\n\n## Precondition (file state)\nThe on-disk tree_sidecar module must write sidecars at sibling paths with no pending-sidecar API. tree_representation.sidecar_path_for must resolve sibling paths for all formats. promote_pending_sidecar_to_final must not exist.\n\n## Required edits (migration-relevant ONLY; do not refactor anything else)\n1. Module docstring: change\n       \"Tests for CST ``.cst/*.tree`` sidecar persistence.\"\n   to\n       \"Tests for CST sibling ``<source>.tree`` sidecar persistence.\"\n\n2. Imports:\n   - Delete the import of TreeOperation, TreeOperationType from\n     code_analysis.core.cst_tree.models (only the removed pending test used\
  \ them).\n   - Delete the import of modify_tree from code_analysis.core.cst_tree.tree_modifier\n     (only the removed pending test used it).\n   - In the tree_sidecar import block, remove sidecar_path_for_py,\n     pending_sidecar_path_for_py, and promote_pending_sidecar_to_final. Keep\n     compute_source_sha256_hex, read_sidecar_payload, render_sidecar_file,\n     tree_to_sidecar_payload, verify_sidecar_against_source, write_sidecar_atomic.\n   - Add, after the tree_sidecar import block:\n         from code_analysis.tree.sibling_convention import sibling_tree_path\n\n3. test_sidecar_path_next_to_py: replace the assertion\n       assert sidecar_path_for_py(p) == tmp_path / \"pkg\" / \".cst\" / \"mod.tree\"\n   with\n       assert sibling_tree_path(p.resolve()) == (tmp_path / \"pkg\" / \"mod.py.tree\").resolve()\n\n4. test_write_read_roundtrip: replace the line\n       raw = sidecar_path_for_py(path).read_text(encoding=\"utf-8\")\n   with\n       raw = sibling_tree_path(path.resolve()).read_text(encoding=\"\
  utf-8\")\n   Leave the rest of the test (write_sidecar_atomic call, header assertion,\n   read_sidecar_payload, sha and verify assertions, finally remove_tree) unchanged.\n\n5. Delete these three test functions entirely; the mechanism they cover no longer\n   exists (write_sidecar_atomic always writes the single sibling\n   path; there is no pending path and no promote step):\n       - test_pending_sidecar_when_py_missing\n       - test_promote_pending_sidecar_after_py_created\n       - test_modify_tree_writes_pending_sidecar_without_py_file\n\n## Forbidden\n- Do not add replacement tests for the deleted pending behaviour.\n- Do not change test_verify_sidecar_sha or test_render_parse_roundtrip_dict.\n- Do not touch other files.\n\n## Result\nAfter the edits the module keeps exactly four tests: test_sidecar_path_next_to_py,\ntest_verify_sidecar_sha, test_write_read_roundtrip, test_render_parse_roundtrip_dict.\n\n## Full current file content (verbatim,\
  \ 136 lines)\n```python\n\"\"\"Tests for CST ``.cst/*.tree`` sidecar persistence.\"\"\"\n\nfrom __future__ import annotations\n\nfrom pathlib import Path\n\nfrom code_analysis.core.cst_tree.models import TreeOperation, TreeOperationType\nfrom code_analysis.core.cst_tree.tree_builder import create_tree_from_code, remove_tree\nfrom code_analysis.core.cst_tree.tree_modifier import modify_tree\nfrom code_analysis.core.cst_tree.tree_sidecar import (\n    compute_source_sha256_hex,\n    pending_sidecar_path_for_py,\n    promote_pending_sidecar_to_final,\n    read_sidecar_payload,\n    render_sidecar_file,\n    sidecar_path_for_py,\n    tree_to_sidecar_payload,\n    verify_sidecar_against_source,\n    write_sidecar_atomic,\n)\n\ndef test_sidecar_path_next_to_py(tmp_path: Path) -> None:\n    p = tmp_path / \"pkg\" / \"mod.py\"\n    assert sidecar_path_for_py(p) == tmp_path / \"pkg\" / \".cst\" / \"mod.tree\"\n\ndef test_verify_sidecar_sha(tmp_path: Path) -> None:\n    path = tmp_path / \"a.py\"\
  \n    src = \"x = 1\\n\"\n    path.write_text(src, encoding=\"utf-8\")\n    tree = create_tree_from_code(str(path), src)\n    try:\n        payload = tree_to_sidecar_payload(tree)\n        assert verify_sidecar_against_source(src, payload)\n        assert not verify_sidecar_against_source(src + \"\\n# touch\\n\", payload)\n    finally:\n        remove_tree(tree.tree_id)\n\ndef test_write_read_roundtrip(tmp_path: Path) -> None:\n    path = tmp_path / \"b.py\"\n    src = \"def f():\\n    return 2\\n\"\n    path.write_text(src, encoding=\"utf-8\")\n    tree = create_tree_from_code(str(path), src)\n    try:\n        write_sidecar_atomic(path, tree)\n        raw = sidecar_path_for_py(path).read_text(encoding=\"utf-8\")\n        assert raw.startswith(\"CST_TREE_V1 sha256=\")\n        loaded = read_sidecar_payload(path)\n        assert loaded is not None\n        assert loaded[\"source_sha256\"] == compute_source_sha256_hex(tree.module.code)\n        assert verify_sidecar_against_source(tree.module.code,\
  \ loaded)\n    finally:\n        remove_tree(tree.tree_id)\n\ndef test_render_parse_roundtrip_dict() -> None:\n    payload = {\n        \"format_version\": 1,\n        \"source_sha256\": \"a\" * 64,\n        \"root_node_id\": \"rid\",\n        \"path_to_node_id\": {\"0\": \"nid\"},\n        \"metadata_map\": {},\n        \"parent_map\": {},\n        \"node_id_aliases\": {},\n    }\n    text = render_sidecar_file(payload)\n    assert text.splitlines()[0] == f\"CST_TREE_V1 sha256={'a' * 64}\"\n\ndef test_pending_sidecar_when_py_missing(tmp_path: Path) -> None:\n    \"\"\"Sidecar is written under ``.cst/_pending/`` before ``.py`` exists on disk.\"\"\"\n    path = tmp_path / \"new_module.py\"\n    src = \"x = 1\\n\"\n    tree = create_tree_from_code(str(path), src)\n    try:\n        assert not path.is_file()\n        written = write_sidecar_atomic(path, tree)\n        pending = pending_sidecar_path_for_py(path)\n        final = sidecar_path_for_py(path)\n        assert written == pending\n\
  \        assert pending.is_file()\n        assert not final.is_file()\n        loaded = read_sidecar_payload(path)\n        assert loaded is not None\n        assert verify_sidecar_against_source(src, loaded)\n    finally:\n        remove_tree(tree.tree_id)\n\ndef test_promote_pending_sidecar_after_py_created(tmp_path: Path) -> None:\n    \"\"\"When ``.py`` appears, pending sidecar is renamed to ``.cst/{stem}.tree``.\"\"\"\n    path = tmp_path / \"later.py\"\n    src = \"def f():\\n    return 1\\n\"\n    tree = create_tree_from_code(str(path), src)\n    try:\n        write_sidecar_atomic(path, tree)\n        pending = pending_sidecar_path_for_py(path)\n        final = sidecar_path_for_py(path)\n        assert pending.is_file()\n\n        path.write_text(src, encoding=\"utf-8\")\n        promoted = promote_pending_sidecar_to_final(path)\n        assert promoted == final\n        assert final.is_file()\n        assert not pending.is_file()\n        assert read_sidecar_payload(path) is not\
  \ None\n    finally:\n        remove_tree(tree.tree_id)\n\ndef test_modify_tree_writes_pending_sidecar_without_py_file(tmp_path: Path) -> None:\n    path = tmp_path / \"draft_only.py\"\n    tree = create_tree_from_code(str(path), \"a = 1\\n\")\n    tree_id = tree.tree_id\n    pending = pending_sidecar_path_for_py(path)\n    try:\n        modify_tree(\n            tree_id,\n            [\n                TreeOperation(\n                    action=TreeOperationType.REPLACE,\n                    node_id=next(\n                        nid\n                        for nid, meta in tree.metadata_map.items()\n                        if meta.type == \"Assign\"\n                    ),\n                    code_lines=[\"a = 2\"],\n                )\n            ],\n        )\n        assert not path.is_file()\n        assert pending.is_file()\n    finally:\n        remove_tree(tree_id)\n```\n"
