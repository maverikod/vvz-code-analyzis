Subject: Chunker API — model and size/window not passed in requests

Hi,

We use your chunker service (svo_client, chunk API) from the code_analysis project. We’re seeing that requests only send the "type" parameter (e.g. type="DocBlock"); we do not pass embedding model name or chunk size/window.

Questions for you:

1. Does the chunk endpoint accept parameters for embedding model and chunk size (or window)? If yes, what are their exact names and expected values?

2. If the service supports them but we don’t send them, does the chunker use server-side defaults, and if so, where are they documented?

3. We need the embedding model name to be returned (or clearly defaulted) so we can store it in our DB for each chunk. Is that already part of the response, or should we expect it in a specific field?

We’re happy to add model/size to our client once we know the correct parameter names and behaviour.

Thanks,
[Your name / team]
