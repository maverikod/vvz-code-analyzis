step_id: A-007
parent_tactical_step: T-001
name: Create git_ops stash module
target_file: code_analysis/core/git_ops/stash_op.py
operation: create_file
priority: 1
depends_on:
- A-002
concepts:
- C-010
object_id: git_local_mutation_operation_set
source_labels:
- '{y5z6}'
prompt: |
  Create the file `code_analysis/core/git_ops/stash_op.py` with EXACTLY the
  following content, byte for byte:

  """Local git stash operations: save (push) the current uncommitted changes
  onto the stash and pop the most recent stash entry back onto the working
  tree. Acts only on the resolved project repository and never contacts a
  remote.
  """

  from code_analysis.core.git_ops.common import ensure_local_repo, run_git


  def git_stash_save(root_dir: str, message: str | None = None) -> dict:
      """Save the current uncommitted changes onto the stash.

      Args:
          root_dir: Absolute path to the resolved project repository root.
          message: Optional stash message. When None, git's default stash
              message is used.

      Returns:
          On success: {"success": True, "stashed": bool} where stashed is
          False when there were no local changes to save (not an error) and
          True when a stash entry was created.
          On failure: {"success": False, "code": str, "message": str} with
          code one of "GIT_NOT_AVAILABLE", "GIT_NOT_A_REPO",
          "GIT_COMMAND_FAILED".
      """
      repo_failure = ensure_local_repo(root_dir)
      if repo_failure is not None:
          return repo_failure

      args = ["stash", "push"]
      if message is not None:
          args.extend(["-m", message])
      returncode, stdout, stderr = run_git(root_dir, args)
      if returncode != 0:
          return {
              "success": False,
              "code": "GIT_COMMAND_FAILED",
              "message": stderr.strip() or "git stash push failed.",
          }
      stashed = "no local changes to save" not in stdout.lower()
      return {"success": True, "stashed": stashed}


  def git_stash_pop(root_dir: str) -> dict:
      """Apply and drop the most recent stash entry onto the working tree.

      Args:
          root_dir: Absolute path to the resolved project repository root.

      Returns:
          On success: {"success": True} indicating the stash entry was applied
          and dropped.
          On failure: {"success": False, "code": str, "message": str} with
          code one of "GIT_NOT_AVAILABLE", "GIT_NOT_A_REPO",
          "GIT_NO_STASH_ENTRY", "GIT_CONFLICT", "GIT_COMMAND_FAILED".
          On GIT_CONFLICT, the working tree is left with conflict markers for
          manual resolution, matching git's own stash-pop conflict behaviour;
          there is no stash-pop abort command.
      """
      repo_failure = ensure_local_repo(root_dir)
      if repo_failure is not None:
          return repo_failure

      returncode, stdout, stderr = run_git(root_dir, ["stash", "pop"])
      if returncode != 0:
          combined = (stdout + stderr).lower()
          if "no stash entries found" in combined:
              return {
                  "success": False,
                  "code": "GIT_NO_STASH_ENTRY",
                  "message": "No stash entries found.",
              }
          if "conflict" in combined:
              return {
                  "success": False,
                  "code": "GIT_CONFLICT",
                  "message": stderr.strip() or "Stash pop produced a conflict.",
              }
          return {
              "success": False,
              "code": "GIT_COMMAND_FAILED",
              "message": stderr.strip() or "git stash pop failed.",
          }
      return {"success": True}

  Constraints:
  - Import exactly `from code_analysis.core.git_ops.common import ensure_local_repo, run_git`.
  - Both functions must call ensure_local_repo(root_dir) first and return its
    failure dict immediately if not None.
  - git_stash_save must treat "no local changes to save" (git's own message
    when there is nothing to stash, returncode 0) as a success with
    stashed=False, not as an error.
  - git_stash_pop must NOT attempt any abort command on conflict (git has no
    stash-pop abort); it must leave the working tree exactly as git itself
    left it and return GIT_CONFLICT.
  - Do not add any other function, class, or module-level variable.

  Exact signatures of the common.py helpers this file imports (quoted
  verbatim, do not deviate):
  - `def run_git(root_dir: str, args: list[str], timeout_seconds: int = 60) -> tuple[int, str, str]:` — runs `git -C <root_dir> <args>` via subprocess, no shell, returns (returncode, stdout, stderr).
  - `def ensure_local_repo(root_dir: str) -> dict | None:` — returns None on success; else {"success": False, "code": "GIT_NOT_AVAILABLE"|"GIT_NOT_A_REPO", "message": str}.

  Context (machine_spec excerpt, concept_id C-010):
  concept_id: C-010
  name: GitLocalMutatingOps
  definition: The local git mutating operations that change the working tree
    or history without contacting a remote.
  properties:
    operations: [add, commit, switch, restore, branch_create, branch_delete,
      stash, reset, merge, cherry_pick, revert, tag]
    queue: false
    contacts_remote: false
  source_labels: ['{y5z6}', '{e1f2}']
  note: This file implements the "stash" operation owned by tactical step
    G-003/T-001 ("stashing and restoring uncommitted changes"). reset and
    revert are owned by sibling tactical steps and are out of scope for this
    file.
verification:
  type: import
  target: code_analysis.core.git_ops.stash_op
  expected: Importing code_analysis.core.git_ops.stash_op succeeds and
    exposes two callables named git_stash_save and git_stash_pop with the
    specified signatures.
status: frozen
