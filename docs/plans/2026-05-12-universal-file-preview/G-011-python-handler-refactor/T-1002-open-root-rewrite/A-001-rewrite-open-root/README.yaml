step_id: A-001
parent_tactical_step: T-1002
name: Rewrite open_root and _metadata_children_to_nodes
target_file: code_analysis/commands/universal_file_preview/handlers/python_handler.py
operation: modify_file
priority: 1
depends_on: []
concepts:
- C-016
- C-022
status: draft
verification:
  type: import
  target: code_analysis.commands.universal_file_preview.handlers.python_handler
  expected: 'Module imports without error. PythonFileHandler.open_root calls render_module
    and returns a Node. _metadata_children_to_nodes iterates metadata_map filtering
    by depth and parent_id, skipping whitespace-only kinds.

    '
prompt: "## Task\n\nModify an existing file.\n\nTarget file: code_analysis/commands/universal_file_preview/handlers/python_handler.py\n\
  Operation: modify_file\n\n## Current file content\n\nThe file exists with exactly\
  \ the following content after dead functions\nwere removed. Produce the complete\
  \ new file with _metadata_children_to_nodes\nand open_root rewritten as described\
  \ below.\n\n---\n\"\"\"\nPythonFileHandler — FileHandler for .py, .pyi, .pyw files\
  \ (C-016).\n\nOpens Python source via the project's cst_load_file infrastructure.\n\
  Root NodeKind is tree_node with type label 'Module'.\nIdentifier format: stable_id\
  \ UUID from cst_load_file outline_nodes.\n\nAuthor: Vasiliy Zdanovskiy\nemail: vasilyvz@gmail.com\n\
  \"\"\"\n\nfrom __future__ import annotations\n\nimport logging\nfrom typing import\
  \ Any\n\nfrom ..base_handler import FileHandler\nfrom ..errors import (\n    PreviewError,\n\
  \    file_structure_error,\n    input_error,\n    INPUT_ERROR_UNKNOWN_NODE_REF,\n\
  )\nfrom ..models import Node, NodeKind\nfrom ..python_visualizer import render_module,\
  \ render_node\nfrom ..budget import PreviewBudget\n\nlogger = logging.getLogger(__name__)\n\
  \n\nclass PythonFileHandler(FileHandler):\n    \"\"\"FileHandler for Python files\
  \ (.py, .pyi, .pyw) (C-016).\n\n    Root NodeKind: tree_node (Module).\n    Interior\
  \ nodes: tree_node for ClassDef and FunctionDef;\n    tree_node or scalar for other\
  \ statements; scalar for leaf values.\n    Identifier source: cst_load_file outline_nodes\
  \ stable_id UUID.\n    Lazy materialisation: only root and requested children are\
  \ loaded.\n\n    Attributes:\n        supported_extensions: Frozenset of .py, .pyi,\
  \ .pyw.\n    \"\"\"\n\n    @property\n    def supported_extensions(self) -> frozenset[str]:\n\
  \        \"\"\"Frozenset of lowercase extensions this handler supports.\"\"\"\n\
  \        return frozenset({\".py\", \".pyi\", \".pyw\"})\n\n    def open_root(\n\
  \        self,\n        file_path: str,\n        session: Any | None,\n        budget:\
  \ PreviewBudget | None = None,\n    ) -> Node | PreviewError:\n        \"\"\"Parse\
  \ the Python file, render module overview via PythonNodeRenderer (C-022),\n    \
  \    and return a tree_node Module root Node carrying the rendered text in attributes.\n\
  \n        Args:\n            file_path: Absolute path to the .py/.pyi/.pyw file.\n\
  \            session: Existing CSTTree session or None.\n            budget: PreviewBudget\
  \ for render_module; defaults to (20, 120) when None.\n\n        Returns:\n    \
  \        Module root Node (NodeKind.TREE_NODE) or PreviewError.\n        \"\"\"\n\
  \        try:\n            from code_analysis.core.cst_tree.tree_builder import\
  \ load_file_to_tree\n            tree = session if session is not None else load_file_to_tree(file_path)\n\
  \            self._last_file_path = file_path\n            self._last_session =\
  \ tree\n            self._last_tree_id = getattr(tree, \"tree_id\", None)\n    \
  \        _budget = budget if budget is not None else PreviewBudget(\n          \
  \      preview_lines=20, value_preview_len=120\n            )\n            text\
  \ = render_module(tree, _budget)\n            root_stable_id = (\n             \
  \   tree.metadata_map[tree.root_node_id].stable_id\n                if tree.root_node_id\
  \ and tree.root_node_id in tree.metadata_map\n                else \"\"\n      \
  \      )\n            return Node(\n                node_kind=NodeKind.TREE_NODE,\n\
  \                node_ref=root_stable_id,\n                type_label=\"Module\"\
  ,\n                name=None,\n                attributes={\"text\": text},\n  \
  \              _children_loader=lambda: _metadata_children_to_nodes(\n         \
  \           tree, tree.root_node_id\n                ),\n            )\n       \
  \ except Exception as exc:\n            return file_structure_error(parser=\"libcst\"\
  , message=str(exc))\n\n    def resolve_node_ref(\n        self, node_ref: str, session:\
  \ Any | None\n    ) -> Node | PreviewError:\n        \"\"\"Resolve a stable_id UUID\
  \ to a Node rendered by PythonNodeRenderer (C-009, C-022).\n\n        Args:\n  \
  \          node_ref: stable_id UUID string from cst_load_file outline_nodes.\n \
  \           session: CSTTree session or None (reopens file if None).\n\n       \
  \ Returns:\n            Resolved Node with rendered text in attributes, or PreviewError.\n\
  \        \"\"\"\n        if session is None:\n            fp = getattr(self, \"\
  _last_file_path\", None)\n            if fp is None:\n                return input_error(\n\
  \                    INPUT_ERROR_UNKNOWN_NODE_REF,\n                    \"No active\
  \ CST session and no cached file_path to scan.\",\n                    details={\"\
  node_ref\": node_ref},\n                )\n            open_result = self.open_root(fp,\
  \ None)\n            if isinstance(open_result, PreviewError):\n               \
  \ return open_result\n            session = getattr(self, \"_last_session\", None)\n\
  \            if session is None:\n                return input_error(\n        \
  \            INPUT_ERROR_UNKNOWN_NODE_REF,\n                    \"Could not open\
  \ CST session for stable_id scan.\",\n                    details={\"node_ref\"\
  : node_ref},\n                )\n        meta = (\n            next(\n         \
  \       (m for m in session.metadata_map.values() if m.stable_id == node_ref),\n\
  \                None,\n            )\n            if hasattr(session, \"metadata_map\"\
  )\n            else None\n        )\n        if meta is None:\n            return\
  \ input_error(\n                INPUT_ERROR_UNKNOWN_NODE_REF,\n                f\"\
  stable_id {node_ref!r} not found in current CST.\",\n                details={\"\
  node_ref\": node_ref},\n            )\n        text = render_node(session, node_ref)\n\
  \        return Node(\n            node_kind=NodeKind.TREE_NODE,\n            node_ref=node_ref,\n\
  \            type_label=meta.type,\n            name=meta.name,\n            attributes={\"\
  text\": text},\n        )\n\n\ndef _metadata_children_to_nodes(tree: Any, parent_node_id:\
  \ str | None) -> list[Node]:\n    \"\"\"Build preview Nodes from CSTTree metadata\
  \ for direct children of parent_node_id.\"\"\"\n    if parent_node_id is None:\n\
  \        return []\n    _MEANINGFUL_KINDS = frozenset({\"import\", \"class\", \"\
  function\", \"method\", \"smallstmt\"})\n    _TREE_NODE_KINDS = frozenset({\"class\"\
  , \"function\", \"method\"})\n    nodes_by_line: list[tuple[int, Any]] = []\n  \
  \  for nid, meta in tree.metadata_map.items():\n        if meta.parent_id != parent_node_id:\n\
  \            continue\n        kind = getattr(meta, \"kind\", \"\") or \"\"\n  \
  \      if kind not in _MEANINGFUL_KINDS:\n            continue\n        nodes_by_line.append((meta.start_line\
  \ or 0, meta))\n    nodes_by_line.sort(key=lambda x: x[0])\n    nodes = []\n   \
  \ for _, meta in nodes_by_line:\n        node_type = meta.type\n        kind_str\
  \ = getattr(meta, \"kind\", \"\") or \"\"\n        preview_kind = NodeKind.TREE_NODE\
  \ if kind_str in _TREE_NODE_KINDS else NodeKind.SCALAR\n        def _make_loader(nid:\
  \ str) -> Any:\n            def _load() -> list[Node]:\n                return _metadata_children_to_nodes(tree,\
  \ nid)\n            return _load\n        has_children = preview_kind == NodeKind.TREE_NODE\n\
  \        nodes.append(Node(\n            node_kind=preview_kind,\n            node_ref=meta.stable_id,\n\
  \            type_label=node_type,\n            name=meta.name,\n            attributes={},\n\
  \            _children_loader=_make_loader(meta.node_id) if has_children else None,\n\
  \        ))\n    return nodes\n---\n\n## What to change\n\n### 1. Rewrite _metadata_children_to_nodes\
  \ body\n\nReplace the entire body (keep the function signature and docstring) with:\n\
  \n    if parent_node_id is None:\n        return []\n    parent_meta = tree.metadata_map.get(parent_node_id)\n\
  \    child_depth = (\n        parent_meta.depth + 1\n        if parent_meta and\
  \ parent_meta.depth is not None\n        else 1\n    )\n    selected: list[tuple[int,\
  \ str, Any]] = []\n    for nid, meta in tree.metadata_map.items():\n        if meta.parent_id\
  \ != parent_node_id:\n            continue\n        if meta.depth != child_depth:\n\
  \            continue\n        kind = meta.kind or \"\"\n        if not kind:\n\
  \            continue\n        selected.append((meta.start_line or 0, nid, meta))\n\
  \    selected.sort(key=lambda x: x[0])\n    nodes: list[Node] = []\n    for _, nid,\
  \ meta in selected:\n        kind_str = meta.kind or \"\"\n        preview_kind\
  \ = (\n            NodeKind.TREE_NODE\n            if kind_str in (\"class\", \"\
  function\", \"method\")\n            else NodeKind.SCALAR\n        )\n        nodes.append(\n\
  \            Node(\n                node_kind=preview_kind,\n                node_ref=meta.stable_id,\n\
  \                type_label=meta.type,\n                name=meta.name,\n      \
  \          attributes={},\n                _children_loader=(\n                \
  \    (lambda nid=nid: _metadata_children_to_nodes(tree, nid))\n                \
  \    if preview_kind == NodeKind.TREE_NODE\n                    else None\n    \
  \            ),\n            )\n        )\n    return nodes\n\nUpdate the _metadata_children_to_nodes\
  \ docstring to:\n    \"\"\"Build preview Nodes from CSTTree metadata for direct\
  \ children of parent_node_id.\n\n    Filters by depth == parent_depth + 1 and non-empty\
  \ kind to skip\n    whitespace and structural nodes.\n\n    Args:\n        tree:\
  \ CSTTree with metadata_map.\n        parent_node_id: node_id of the parent node,\
  \ or None.\n\n    Returns:\n        List of Nodes sorted by start_line.\n    \"\"\
  \"\n\n### 2. Keep open_root and resolve_node_ref exactly as in current file above\n\
  \nDo not change them.\n\n## Constraints\n\nProduce the full new file content.\n\
  File must not exceed 180 lines after the change.\n"
