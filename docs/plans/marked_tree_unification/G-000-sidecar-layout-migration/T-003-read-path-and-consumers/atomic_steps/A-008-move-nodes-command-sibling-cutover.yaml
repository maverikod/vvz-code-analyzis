step_id: A-008
parent_tactical_step: T-003
name: Cut move_nodes_command.py sidecar path resolution over to sibling convention
target_file: code_analysis/commands/universal_file_edit/move_nodes_command.py
operation: modify_file
priority: 8
depends_on: []
concepts:
- C-023
- C-003
status: ready_for_review
verification:
  type: static_analysis
  target: code_analysis/commands/universal_file_edit/move_nodes_command.py
  expected: black, flake8, mypy exit 0; module no longer imports or calls sidecar_path_for_py and instead imports sibling_tree_path; _run_sidecar_move_batch resolves tmp_sidecar and orig_sidecar via sibling_tree_path(<path>.resolve()); the Step 7 commit block still renames tmp_sidecar to orig_sidecar; grep for 'sidecar_path_for_py' and '.cst' in this file returns nothing.
prompt: "Operation: modify_file\nTarget: code_analysis/commands/universal_file_edit/move_nodes_command.py\n\n## Goal\nCut the Python sidecar path resolution over to the sibling convention (C-003).\nsidecar_path_for_py (which produced <parent>/.cst/<stem>.tree) is no longer
  exported from tree_sidecar.py. This module resolves the sidecar path in _run_sidecar_move_batch torename the tree file alongside the moved source; replace both resolver calls with\nsibling_tree_path and drop the dead import. No logic changes beyond the path source.\n\n## MRS excerpt\n\n**C-023 SidecarLayoutMigration**\n- definition: The sidecar layout cutover (SidecarLayoutMigration, C-023) that redirects the remaining legacy tree-path call sites (not reached by consumer integration) onto the existing sibling_tree_path function and removes the three legacy layouts (.cst/ subdirectory, .trees/ mirror directory, .tree_sidecar adjacent file). It does not create sibling_tree_path; that function already exists from the\
  \ storage work. Runs after G-001 through G-005 are built and verified end-to-end; this is the final layout cutover with no subsequent global step.\n- properties:\n  - final sidecar cutover after G-001..G-005 verification; no subsequent global step; NodeIdMap (C-025) UUID4 identity in MAP section\n  - replaces three independent legacy path resolvers (not delegates of one another) with sibling_tree_path: sidecar_path_for_py (.cst/) in cst_tree/tree_sidecar.py, the standalone resolve_trees_sidecar_path module (.trees/) in core/tree_temp/sidecar_paths.py which is removed whole, and the inline .tree_sidecar branch of the format-dispatch sidecar_path_for in tree_representation.py\n  - redirects write_sidecar_atomic to write at the sibling path\n  - removes the legacy pending-sidecar mechanism entirely (pending_sidecar_path_for_py, resolve_sidecar_write_path, promote_pending_sidecar_to_final, _cleanup_empty_pending_dir, the _pending subdirectory, and the is_file branch\
  \ in write_sidecar_atomic); it is deleted, not migrated, because the edit-session lifecycle removes the pre-session no-source state\n  - redirects the reader functions in tree_representation.py to resolve the sibling path\n  - redirects the direct legacy-path consumers onto sibling_tree_path: tree_builder, tree_saver, stable_tree, grep_block_resolver (.cst/), and tree_temp_write_commit, tree_temp_open_support (.trees/), so JSON and YAML tree files become siblings like every other format\n  - ensures TreeRepresentationRef carries the sibling path after validate_or_recreate_tree_file\n  - replaces legacy path assertions in tests with sibling path assertions\n  - hard cutover: no backward-compatibility shim\n\n**C-003 TreeFile**\n- definition: The on-disk artifact holding source content with node-id markers embedded natively per format.\n- properties:\n  - contains source content plus markers\n  - stored as a sibling file placed directly next to the source file it describes\n  - on-disk format\
  \ is natively-marked content, not a separate JSON payload\n\n## Dependency (already exists from tree storage and lifecycle (sibling storage convention))\ncode_analysis/tree/sibling_convention.py provides:\n    def sibling_tree_path(source_abs: Path) -> Path\nmapping <dir>/<stem>.<ext> -> <dir>/<stem>.<ext>.tree (full filename plus \".tree\";\ne.g. foo.py -> foo.py.tree, foo.py.tmp -> foo.py.tmp.tree).\n\n## Required edits (migration-relevant ONLY; do not refactor anything else)\n1. Delete the import line:\n       from code_analysis.core.cst_tree.tree_sidecar import sidecar_path_for_py\n   Add, immediately after the tree_builder import line\n   (from code_analysis.core.cst_tree.tree_builder import get_tree, load_file_to_tree):\n       from code_analysis.tree.sibling_convention import sibling_tree_path\n\n2. In _run_sidecar_move_batch, Step 2 block, replace the two resolver calls:\n       tmp_sidecar = sidecar_path_for_py(tmp_path)\n       orig_sidecar = sidecar_path_for_py(orig_path)\n\
  \   with:\n       tmp_sidecar = sibling_tree_path(tmp_path.resolve())\n       orig_sidecar = sibling_tree_path(orig_path.resolve())\n   Leave everything else in the function unchanged: the _cleanup_tmp closure\n   (unlinks tmp_path and tmp_sidecar), the shutil.copy2 copy, both\n   load_file_to_tree calls, the dataclasses.replace tmp_session, the delete/insert\n   batches via run_sidecar_cst_edit_batch, the compile() validation, the Step 7\n   commit block (os.replace tmp_path->orig_path, then when tmp_sidecar.exists():\n   tmp_sidecar.parent.mkdir(parents=True, exist_ok=True) and\n   os.replace tmp_sidecar->orig_sidecar), and Step 8 reload. The mkdir is now a\n   no-op on an already-existing source directory but must stay.\n\n## Forbidden\n- No backward-compatibility fallback to .cst/ paths.\n- Do not change run_sidecar_cst_edit_batch usage, the compile() gate, the\n  UniversalFileMoveNodesCommand class (get_name, get_schema, metadata,\n  validate_params, execute), _sorted_node_ids_by_tree_order,\
  \ or any other code.\n- Do not touch other files.\n\n## Full current file content (verbatim, 381 lines)\n```python\n\"\"\"\nuniversal_file_move_nodes: move a block of sibling CST nodes within a session.\n\nAuthor: Vasiliy Zdanovskiy\nemail: vasilyvz@gmail.com\n\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nimport os\nfrom dataclasses import replace\nfrom pathlib import Path\nfrom typing import Any, Dict, List, Optional, Type, cast\n\nfrom mcp_proxy_adapter.commands.result import ErrorResult, SuccessResult\n\nfrom code_analysis.commands.base_mcp_command import BaseMCPCommand\nfrom code_analysis.commands.universal_file_edit.errors import (\n    PARSE_ERROR,\n    SESSION_NOT_FOUND,\n    WRITE_FAILED,\n    error_result_for_edit,\n    error_result_from_make_error,\n    make_error,\n)\nfrom code_analysis.commands.universal_file_edit.format_group import FORMAT_SIDECAR\nfrom code_analysis.commands.universal_file_edit.session import EditSession, get_session\nfrom code_analysis.commands.universal_file_edit.sidecar_cst_apply\
  \ import (\n    run_sidecar_cst_edit_batch,\n)\nfrom code_analysis.commands.universal_file_edit.move_nodes_command_metadata import (\n    get_universal_file_move_nodes_metadata,\n)\nfrom code_analysis.core.cst_tree.models import CSTTree, ROOT_NODE_ID_SENTINEL\nfrom code_analysis.core.cst_tree.node_stable_id import logical_source_from_module\nfrom code_analysis.core.cst_tree.tree_builder import get_tree, load_file_to_tree\nfrom code_analysis.core.cst_tree.tree_sidecar import sidecar_path_for_py\n\ndef _sorted_node_ids_by_tree_order(\n    tree: CSTTree, node_ids: List[str]\n) -> List[str]:\n    \"\"\"Return node_ids sorted by start_line in the tree (source order).\"\"\"\n\n    def _line(nid: str) -> int:\n        meta = tree.metadata_map.get(nid)\n        return meta.start_line if meta else 0\n\n    return sorted(node_ids, key=_line)\n\ndef _run_sidecar_move_batch(\n    session: EditSession,\n    source_node_ids: List[str],\n    target_node_id: Optional[str],\n    parent_node_id: Optional[str],\n\
  \    position: str,\n) -> SuccessResult | ErrorResult:\n    \"\"\"Move source nodes to a new position using a copy of the source file.\n\n    All mutations happen on a .py.tmp copy. The original file is only\n    replaced after successful validation via compile(). On failure the\n    temp files are deleted and the original session is untouched.\n    \"\"\"\n    # --- Resolve original tree ---\n    tid = session.tree_id\n    tree = get_tree(tid) if tid else None\n    if tree is None:\n        try:\n            tree = load_file_to_tree(str(session.abs_path))\n        except FileNotFoundError as exc:\n            return error_result_for_edit(\n                str(exc), \"FILE_NOT_FOUND\", {\"path\": str(session.abs_path)}\n            )\n        except Exception as exc:\n            return error_result_for_edit(\n                str(exc), PARSE_ERROR, {\"path\": str(session.abs_path)}\n            )\n    session.tree_id = tree.tree_id\n\n    # --- Predcheck: all source_node_ids exist ---\n\
  \    for nid in source_node_ids:\n        if nid not in tree.metadata_map:\n            return error_result_for_edit(\n                f\"Node not found: {nid}\",\n                \"STALE_NODE_ID\",\n                {\"stable_id\": nid, \"hint\": \"Re-call universal_file_preview with session_id.\"},\n            )\n\n    # --- Step 1: remember source text for each node (before any mutation) ---\n    ordered_ids = _sorted_node_ids_by_tree_order(tree, source_node_ids)\n    node_codes: List[str] = []\n    for nid in ordered_ids:\n        node = tree.node_map.get(nid)\n        if node is None:\n            return error_result_for_edit(\n                f\"Node object missing for id: {nid}\",\n                \"STALE_NODE_ID\",\n                {\"stable_id\": nid},\n            )\n        node_codes.append(tree.module.code_for_node(node))\n\n    combined_code = \"\".join(node_codes)\n\n    # --- Step 2: copy the source file to .py.tmp ---\n    orig_path = session.abs_path\n    tmp_path = orig_path.with_suffix(\"\
  .py.tmp\")\n    tmp_sidecar = sidecar_path_for_py(tmp_path)\n    orig_sidecar = sidecar_path_for_py(orig_path)\n\n    def _cleanup_tmp() -> None:\n        try:\n            tmp_path.unlink(missing_ok=True)\n        except Exception:\n            pass\n        try:\n            tmp_sidecar.unlink(missing_ok=True)\n        except Exception:\n            pass\n\n    try:\n        import shutil\n        shutil.copy2(str(orig_path), str(tmp_path))\n    except Exception as exc:\n        return error_result_for_edit(\n            str(exc), WRITE_FAILED, {\"path\": str(tmp_path)}\n        )\n\n    # --- Step 3: load tree from the copy ---\n    try:\n        tmp_tree = load_file_to_tree(str(tmp_path))\n    except Exception as exc:\n        _cleanup_tmp()\n        return error_result_for_edit(\n            str(exc), PARSE_ERROR, {\"path\": str(tmp_path)}\n        )\n\n    # --- Step 4: build a temporary session pointing at the copy ---\n    # replace() is dataclasses.replace — creates a copy with\
  \ abs_path = tmp_path.\n    # tree_id is updated to the tmp tree.\n    tmp_session = replace(session, abs_path=tmp_path, tree_id=tmp_tree.tree_id)\n\n    # --- Step 5: apply DELETE ops then INSERT via run_sidecar_cst_edit_batch ---\n    # DELETE all source nodes (bottom-up order is handled inside the batch).\n    delete_ops: List[Dict[str, Any]] = [\n        {\"type\": \"delete\", \"node_id\": nid} for nid in ordered_ids\n    ]\n    result = run_sidecar_cst_edit_batch(tmp_session, delete_ops)\n    if isinstance(result, ErrorResult):\n        _cleanup_tmp()\n        return result\n\n    # INSERT combined code at the target position.\n    if target_node_id is not None:\n        insert_op: Dict[str, Any] = {\n            \"type\": \"insert\",\n            \"target_node_id\": target_node_id,\n            \"position\": position,\n            \"code\": combined_code,\n        }\n    else:\n        insert_op = {\n            \"type\": \"insert\",\n            \"parent_node_id\": parent_node_id\
  \ or ROOT_NODE_ID_SENTINEL,\n            \"position\": position,\n            \"code\": combined_code,\n        }\n    result = run_sidecar_cst_edit_batch(tmp_session, [insert_op])\n    if isinstance(result, ErrorResult):\n        _cleanup_tmp()\n        return result\n\n    # --- Step 6: validate via compile() ---\n    tmp_tree_after = get_tree(tmp_session.tree_id)\n    if tmp_tree_after is None:\n        _cleanup_tmp()\n        return error_result_for_edit(\n            \"Tmp tree lost after operations\", \"INVALID_OPERATION\", {}\n        )\n    source_after = logical_source_from_module(tmp_tree_after.module)\n    try:\n        compile(source_after, str(tmp_path), \"exec\")\n    except SyntaxError as exc:\n        _cleanup_tmp()\n        return error_result_for_edit(\n            f\"Move produced invalid Python: SyntaxError at line {exc.lineno}: {exc.msg}\",\n            \"INVALID_OPERATION\",\n            {\"syntax_error\": str(exc), \"source_node_ids\": source_node_ids},\n       \
  \ )\n\n    # --- Step 7: commit --- atomic rename of file and sidecar ---\n    try:\n        os.replace(str(tmp_path), str(orig_path))\n        if tmp_sidecar.exists():\n            tmp_sidecar.parent.mkdir(parents=True, exist_ok=True)\n            os.replace(str(tmp_sidecar), str(orig_sidecar))\n    except Exception as exc:\n        _cleanup_tmp()\n        return error_result_for_edit(\n            str(exc), WRITE_FAILED, {\"path\": str(orig_path)}\n        )\n\n    # --- Step 8: reload original session tree from updated file ---\n    try:\n        new_tree = load_file_to_tree(str(orig_path))\n        session.tree_id = new_tree.tree_id\n    except Exception:\n        pass  # session will reload on next access\n\n    return SuccessResult(data={\"success\": True, \"updated\": True, \"moved\": len(ordered_ids)})\n\nclass UniversalFileMoveNodesCommand(BaseMCPCommand):\n    \"\"\"MCP command that moves a block of sibling CST nodes to a new position.\n\n    Works within an open universal_file_edit\
  \ session (Python/sidecar only).\n    All mutations happen on a temporary .py.tmp copy. The original file is\n    replaced atomically only after compile() validation succeeds.\n    \"\"\"\n\n    name = \"universal_file_move_nodes\"\n\n    version = \"1.0.0\"\n\n    descr = \"Move a block of sibling CST nodes to a new position in the same file session.\"\n\n    category = \"file_management\"\n\n    author = \"Vasiliy Zdanovskiy\"\n\n    email = \"vasilyvz@gmail.com\"\n\n    use_queue = False\n\n    @staticmethod\n    def get_name() -> str:\n        \"\"\"Return the MCP command name.\n\n        Returns:\n            MCP command name string.\n        \"\"\"\n        return \"universal_file_move_nodes\"\n\n    @classmethod\n    def get_schema(cls) -> Dict[str, Any]:\n        \"\"\"Return the JSON schema for command parameters.\n\n        Returns:\n            JSON schema dict describing project_id, session_id, source_node_ids,\n            target_node_id, parent_node_id, position.\n      \
  \  \"\"\"\n        return {\n            \"type\": \"object\",\n            \"properties\": {\n                \"project_id\": {\n                    \"type\": \"string\",\n                    \"description\": \"Project UUID. Use list_projects to discover valid project_id values.\",\n                },\n                \"session_id\": {\n                    \"type\": \"string\",\n                    \"description\": \"Active session UUID returned by universal_file_open.\",\n                },\n                \"source_node_ids\": {\n                    \"type\": \"array\",\n                    \"items\": {\"type\": \"string\"},\n                    \"minItems\": 1,\n                    \"description\": (\n                        \"Stable UUIDs of nodes to move (node_ref from universal_file_preview). \"\n                        \"Caller-supplied order is ignored; nodes are moved in source order (by start_line).\"\n                    ),\n                },\n                \"target_node_id\"\
  : {\n                    \"type\": \"string\",\n                    \"description\": (\n                        \"Sibling anchor: insert block before or after this node. \"\n                        \"Mutually exclusive with parent_node_id.\"\n                    ),\n                },\n                \"parent_node_id\": {\n                    \"type\": \"string\",\n                    \"description\": (\n                        \"Container anchor: insert block as first or last child. \"\n                        \"Use __root__ for module level. \"\n                        \"Mutually exclusive with target_node_id.\"\n                    ),\n                },\n                \"position\": {\n                    \"type\": \"string\",\n                    \"enum\": [\"before\", \"after\", \"first\", \"last\"],\n                    \"description\": (\n                        \"With target_node_id: before|after. \"\n                        \"With parent_node_id: first|last.\"\n           \
  \         ),\n                    \"default\": \"after\",\n                },\n            },\n            \"required\": [\"project_id\", \"session_id\", \"source_node_ids\"],\n            \"additionalProperties\": False,\n        }\n\n    @classmethod\n    def metadata(cls: Type[\"UniversalFileMoveNodesCommand\"]) -> Dict[str, Any]:\n        \"\"\"Return extended AI/docs metadata for universal_file_move_nodes.\n\n        Returns:\n            Metadata dict with description, parameters, examples, errors.\n        \"\"\"\n        return cast(Dict[str, Any], get_universal_file_move_nodes_metadata(cls))\n\n    def validate_params(self, params: Dict[str, Any]) -> Dict[str, Any]:\n        \"\"\"Validate parameters semantically beyond JSON schema constraints.\n\n        Args:\n            params: Raw parameter dict from the MCP adapter.\n\n        Returns:\n            Validated and normalised parameter dict.\n        \"\"\"\n        params = super().validate_params(params)\n        target =\
  \ params.get(\"target_node_id\")\n        parent = params.get(\"parent_node_id\")\n        if target is not None and parent is not None:\n            raise ValueError(\n                \"Provide either target_node_id or parent_node_id, not both.\"\n            )\n        return params\n\n    async def execute(  # type: ignore[override]\n        self,\n        project_id: str,\n        session_id: str,\n        source_node_ids: List[str],\n        target_node_id: Optional[str] = None,\n        parent_node_id: Optional[str] = None,\n        position: str = \"after\",\n        **kwargs: Any,\n    ) -> SuccessResult | ErrorResult:\n        \"\"\"Execute move_nodes on the session draft.\n\n        Args:\n            project_id: Project UUID (reserved for future validation).\n            session_id: Active session identifier.\n            source_node_ids: Stable UUIDs of nodes to move.\n            target_node_id: Sibling anchor node UUID (before|after).\n            parent_node_id: Container\
  \ node UUID (first|last).\n            position: Insertion position (before|after|first|last).\n            **kwargs: Adapter context.\n\n        Returns:\n            SuccessResult with moved count, or ErrorResult on failure.\n        \"\"\"\n        del project_id, kwargs\n\n        if target_node_id is None and parent_node_id is None:\n            parent_node_id = ROOT_NODE_ID_SENTINEL\n            position = \"last\"\n\n        try:\n            session = get_session(session_id)\n        except ValueError:\n            return error_result_from_make_error(\n                make_error(SESSION_NOT_FOUND, f\"Unknown session: {session_id}\")\n            )\n\n        if session.format_group != FORMAT_SIDECAR:\n            return error_result_from_make_error(\n                make_error(\n                    \"INVALID_OPERATION\",\n                    \"universal_file_move_nodes only supports Python (.py) sidecar sessions.\",\n                )\n            )\n\n        return await asyncio.to_thread(\n\
  \            _run_sidecar_move_batch,\n            session,\n            source_node_ids,\n            target_node_id,\n            parent_node_id,\n            position,\n        )\n```\n"
