concepts:
- C-001
- C-002
- C-003
- C-004
- C-005
- C-009
depends_on: []
name: Create session_list_file_locks_command.py
operation: create_file
parent_tactical_step: T-002
priority: 3
prompt: "Create a new Python file at the exact path:
  code_analysis/commands/sessions/session_list_file_locks_command.py

This file is new with no pre-existing content.

MRS context:
- C-002 SessionFileLock: queried via list_session_file_locks.
- C-003 SessionTouchRule: touch_or_error first; SESSION_NOT_FOUND if absent.
- C-005 SessionMCPCommands: session_list_file_locks; touch then policy check then list.
- C-009 SecurityPolicy: enforce_security_policy called after touch_or_error
  and before list_session_file_locks.

Execution order:
  1. touch_or_error(database, session_id) -> SESSION_NOT_FOUND on failure.
  2. enforce_security_policy(...) -> COMMAND_FORBIDDEN on failure.
  3. list_session_file_locks(...).

File-level docstring:
\"\"\"\nsession_list_file_locks MCP command: list file locks held by a session.\n\nAuthor:
  Vasiliy Zdanovskiy\nemail: vasilyvz@gmail.com\n\"\"\"

Imports (exact):
  from __future__ import annotations
  from typing import Any, Dict
  from mcp_proxy_adapter.commands.result import ErrorResult, SuccessResult
  from code_analysis.commands.base_mcp_command import BaseMCPCommand
  from code_analysis.core.client_sessions import (
      list_session_file_locks,
      touch_or_error,
      SessionNotFoundError,
  )
  from code_analysis.core.security_policy_guard import (
      enforce_security_policy,
      CommandForbiddenError,
  )

Class:

class SessionListFileLocksCommand(BaseMCPCommand):
    \"\"\"MCP command: list file locks held by a session.\"\"\"

    name = 'session_list_file_locks'
    version = '1.0.0'
    descr = 'List all file locks held by a session.'
    category = 'session_management'
    author = 'Vasiliy Zdanovskiy'
    email = 'vasilyvz@gmail.com'
    use_queue = False

    @staticmethod
    def get_name() -> str:
        \"\"\"Return the MCP command name.

        Returns:
            MCP command name string.
        \"\"\"
        return 'session_list_file_locks'

    @classmethod
    def get_schema(cls) -> Dict[str, Any]:
        \"\"\"Return the JSON schema for command parameters.

        Returns:
            JSON schema dict.
        \"\"\"
        return {
            'type': 'object',
            'properties': {
                'session_id': {
                    'type': 'string',
                    'description': 'UUID4 of the session to inspect.',
                },
            },
            'required': ['session_id'],
            'additionalProperties': False,
        }

    async def execute(  # type: ignore[override]
        self,
        session_id: str,
        **kwargs: Any,
    ) -> SuccessResult | ErrorResult:
        \"\"\"Execute session_list_file_locks.

        Execution order: SessionTouchRule -> SecurityPolicy check -> list locks.

        Args:
            session_id: UUID4 of the session to inspect.
            **kwargs: Adapter context (ignored).

        Returns:
            SuccessResult with session_id, locks list, and count.
            ErrorResult SESSION_NOT_FOUND if session is absent.
            ErrorResult COMMAND_FORBIDDEN if SecurityPolicy denies the command.
        \"\"\"
        _ = kwargs
        database = self._open_database_from_config()
        raw_config = self._get_raw_config()
        server_uuid: str = raw_config.get('registration', {}).get('instance_uuid', '')
        policy_mode: str = (raw_config.get('security') or {}).get('policy', 'disabled')

        try:
            touch_or_error(database, session_id)
        except SessionNotFoundError:
            return ErrorResult(
                code='SESSION_NOT_FOUND',
                message=f'Session {session_id!r} not found.',
            )
        try:
            enforce_security_policy(
                database=database,
                session_id=session_id,
                command_name='session_list_file_locks',
                server_uuid=server_uuid,
                policy_mode=policy_mode,
            )
        except CommandForbiddenError as e:
            return ErrorResult(code='COMMAND_FORBIDDEN', message=str(e))

        locks = list_session_file_locks(database, session_id=session_id)
        return SuccessResult(data={
            'session_id': session_id,
            'locks': locks,
            'count': len(locks),
        })

Constraints:
- File must not exceed 110 lines.
- All type annotations required.
- Google-style docstrings."
status: draft
step_id: A-003
target_file: code_analysis/commands/sessions/session_list_file_locks_command.py
verification:
  expected: SessionListFileLocksCommand is importable. get_name() returns
    'session_list_file_locks'. get_schema() has required=['session_id'].
    enforce_security_policy is called between touch_or_error and
    list_session_file_locks.
  target: code_analysis/commands/sessions/session_list_file_locks_command.py
  type: import
