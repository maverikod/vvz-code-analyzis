step_id: A-001
parent_tactical_step: T-802
name: Add universal_file_preview to READ_ONLY_BATCH_WHITELIST
target_file: code_analysis/commands/read_only_batch_whitelist.py
operation: modify_file
priority: 1
depends_on: []
concepts:
- C-001
- C-015
status: done
prompt: |
  Modify an existing Python file.

  PRECONDITION: This step must only be executed after the T-801 stability
  gate has been confirmed (all criteria TRUE). Do not execute before that.

  Target file (modify_file):
    code_analysis/commands/read_only_batch_whitelist.py

  Current content of the file (full, 71 lines):
    """
    Hardcoded read-only command whitelist and validation for batch invocation.

    Author: Vasiliy Zdanovskiy
    email: vasilyvz@gmail.com
    """

    from __future__ import annotations

    from typing import Optional

    READ_ONLY_BATCH_WHITELIST: frozenset[str] = frozenset(
        {
            "ast_statistics",
            "find_classes",
            "find_dependencies",
            "find_usages",
            "fulltext_search",
            "get_ast",
            "get_class_hierarchy",
            "get_code_entity_info",
            "get_entity_dependencies",
            "get_entity_dependents",
            "list_class_methods",
            "list_code_entities",
        }
    )

    ERROR_CODE_NOT_WHITELISTED = "BATCH_COMMAND_NOT_WHITELISTED"


    def validate_command(command_name: str) -> tuple[bool, Optional[dict]]:
        """
        Validate a command name against the read-only batch whitelist.

        Args:
            command_name: The command name to check (e.g. from batch request).

        Returns:
            (True, None) if the command is whitelisted.
            (False, error_payload) if the command is not whitelisted. error_payload
            is a dict with "error", "error_code", "command", "message" for
            deterministic rejection of non-whitelisted or mutating commands.
        """
        if not command_name or not isinstance(command_name, str):
            payload = _make_error_payload(
                str(command_name),
                "Command name must be a non-empty string.",
            )
            return False, payload
        name = command_name.strip()
        if not name:
            payload = _make_error_payload(command_name, "Command name must be non-empty.")
            return False, payload
        if name in READ_ONLY_BATCH_WHITELIST:
            return True, None
        payload = _make_error_payload(
            name,
            "Command is not in the read-only batch whitelist.",
        )
        return False, payload


    def _make_error_payload(command: str, message: str) -> dict:
        """Build explicit error payload for non-whitelisted command."""
        return {
            "error": message,
            "error_code": ERROR_CODE_NOT_WHITELISTED,
            "command": command,
            "message": message,
        }

  Change to make:
    Add the string "universal_file_preview" as a new entry in the
    READ_ONLY_BATCH_WHITELIST frozenset literal, in alphabetical order
    (after "list_code_entities", before the closing brace).

    The resulting frozenset literal must contain exactly these entries:
      "ast_statistics"
      "find_classes"
      "find_dependencies"
      "find_usages"
      "fulltext_search"
      "get_ast"
      "get_class_hierarchy"
      "get_code_entity_info"
      "get_entity_dependencies"
      "get_entity_dependents"
      "list_class_methods"
      "list_code_entities"
      "universal_file_preview"

  No other changes to the file. The ERROR_CODE_NOT_WHITELISTED constant
  and the validate_command function remain unchanged.

  After the change, the test in tests/test_read_only_batch_whitelist.py
  asserts len(READ_ONLY_BATCH_WHITELIST) >= 8 which will still pass
  (new count is 13).
verification:
  type: pytest
  target: tests/test_read_only_batch_whitelist.py
  expected: All existing tests in test_read_only_batch_whitelist.py pass; 'universal_file_preview'
    is in READ_ONLY_BATCH_WHITELIST; validate_command('universal_file_preview') returns
    (True, None).