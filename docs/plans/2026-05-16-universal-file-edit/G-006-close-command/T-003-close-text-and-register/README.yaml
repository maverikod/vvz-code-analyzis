step_id: T-003
parent_global_step: G-006
name: Close text group session and register CloseCommand
description: Implement CloseCommand (C-010) for text group sessions. Delete
  <file>.draft if present. Delete <file>.write if present. Call
  release_session(session_id) to remove the EditSession (C-001) from the
  registry. No draft_rebuilt flag for text group. Also register
  universal_file_close in hooks.py so the command is callable.
concepts:
- C-010
- C-001
inputs:
- name: session
  type: EditSession
  description: Active text group session to close.
outputs:
- name: success
  type: bool
  description: True when session released and artefacts cleaned.
atomic_steps: []
status: draft