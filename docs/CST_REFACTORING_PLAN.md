# Plan: CST Tools Refactoring

**Author**: Vasiliy Zdanovskiy  
**Email**: vasilyvz@gmail.com  
**Date**: 2025-01-12  
**Goal**: Упростить использование CST инструментов через императивный подход с прямым доступом к узлам

---

## 📊 Current State Analysis

### Текущие команды (удалить):

1. **`compose_cst_module`** (`cst_compose_module_command.py`)
   - Сложная структура с множеством типов селекторов
   - Требует промежуточных команд (`list_cst_blocks`, `query_cst`)
   - Сложная валидация и обработка ошибок
   - **Статус**: ❌ УДАЛИТЬ

2. **`list_cst_blocks`** (`list_cst_blocks_command.py`)
   - Возвращает только логические блоки (functions/classes/methods)
   - Используется только для получения ID для `compose_cst_module`
   - **Статус**: ❌ УДАЛИТЬ (заменяется на `cst_load_file`)

3. **`query_cst`** (`query_cst_command.py`)
   - Сложный CSTQuery синтаксис
   - Используется для поиска узлов перед модификацией
   - **Статус**: ⚠️ УПРОСТИТЬ или заменить на `cst_find_node`

### Новые команды (добавить):

1. **`cst_load_file`** - загрузка файла в CST дерево
   - Читает файл с диска
   - Парсит в CST дерево
   - **Сохраняет дерево на сервере** (in-memory хранилище)
   - Возвращает tree_id и **только метаданные узлов** (не полное дерево)
   - Опционально: фильтр узлов для возврата (только нужные типы/уровни)
   - **Статус**: ✅ ДОБАВИТЬ

2. **`cst_modify_tree`** - модификация дерева по node_id
   - Принимает tree_id и операции
   - Работает напрямую с узлами через ID
   - Поддерживает batch operations
   - **Статус**: ✅ ДОБАВИТЬ

3. **`cst_save_tree`** - сохранение дерева в файл
   - Принимает tree_id
   - Валидирует через compile()
   - Сохраняет с backup и git commit
   - **Статус**: ✅ ДОБАВИТЬ

4. **`cst_find_node`** - поиск узлов (простой и XPath-подобный)
   - Простой поиск по типу/имени
   - **XPath-подобный поиск** (используя CSTQuery синтаксис)
   - Поиск по дереву, сохраненному на сервере (tree_id)
   - Возвращает node_id и метаданные найденных узлов
   - **Статус**: ✅ ДОБАВИТЬ (может заменить query_cst)

5. **`cst_get_node_info`** - получение информации о узле
   - Дети, родитель, соседи
   - Метаданные узла
   - **Статус**: ✅ ДОБАВИТЬ (опционально)

---

## 🎯 Refactoring Plan (Step-by-Step)

### Phase 1: Core Infrastructure

#### 1.1. Создать новую модель данных для tree_id
- [ ] Создать `code_analysis/core/cst_tree/models.py`
  - `CSTTree` - класс для хранения дерева с ID (полное CST дерево на сервере)
  - `TreeNodeMetadata` - представление узла с ID и метаданными (для передачи клиенту)
  - `TreeOperation` - операции модификации (replace, insert, remove)
- [ ] Реализовать tree_id генерацию (UUID)
- [ ] Реализовать in-memory хранилище деревьев (dict с tree_id)

**✅ УЖЕ ЕСТЬ**: 
- Lark parser (`cst_query/parser.py`) - готов к использованию
- Executor (`cst_query/executor.py`) - нужно только добавить `query_tree()` для работы с деревом
  - **Ключевая особенность**: полное дерево хранится на сервере
  - Клиент получает только метаданные узлов (node_id, type, name, position)

#### 1.2. Создать утилиты для работы с деревом
- [ ] `code_analysis/core/cst_tree/tree_builder.py`
  - Функция `load_file_to_tree(file_path) -> CSTTree`
  - Парсинг файла в CST дерево
  - Генерация node_id для всех узлов
  - Сохранение в in-memory хранилище
- [ ] `code_analysis/core/cst_tree/tree_modifier.py`
  - Функция `modify_tree(tree_id, operations) -> CSTTree`
  - **Транзакционный подход**: валидация всех операций перед применением
  - **Атомарность**: либо все операции применяются, либо ничего
  - Rollback при ошибках
  - Применение операций к дереву
- [ ] `code_analysis/core/cst_tree/tree_saver.py`
  - Функция `save_tree_to_file(tree_id, file_path, **options) -> str`
  - **Атомарная запись**:
    1. Валидация всего файла через compile() перед записью
    2. Создание backup (если файл существует)
    3. Запись в temp file
    4. Атомарная замена через `os.replace()` или `Path.replace()`
    5. Обновление базы данных в транзакции
    6. Git commit (если требуется)
  - Rollback при ошибках (восстановление из backup)
- [ ] `code_analysis/core/cst_tree/tree_finder.py`
  - Функция `find_nodes(tree_id, query, search_type) -> list[TreeNodeMetadata]`
  - Простой поиск (по типу, имени, позиции)
  - **XPath-подобный поиск** (используя существующий `cst_query/`)
  - **Добавить функцию `query_tree()` в `cst_query/executor.py`**:
    - Текущий `query_source(source: str, selector: str)` парсит source → module
    - Нужна версия `query_tree(module: cst.Module, selector: str)` для работы с уже распарсенным деревом
    - Использовать существующую логику из `query_source()` (строки 88-112)
    - Просто вынести часть кода, которая работает с module
  - Возврат только метаданных найденных узлов
- [ ] `code_analysis/core/cst_tree/tree_metadata.py`
  - Функция `get_node_metadata(tree_id, node_id, **options) -> TreeNodeMetadata`
  - Функция `get_node_children(tree_id, node_id, **options) -> list[TreeNodeMetadata]`
  - Функция `get_node_parent(tree_id, node_id) -> TreeNodeMetadata | None`
  - Извлечение метаданных из дерева на сервере

---

### Phase 2: New Commands Implementation

#### 2.1. Команда `cst_load_file`
- [ ] Создать `code_analysis/commands/cst_load_file_command.py`
- [ ] Реализовать загрузку файла в дерево
- [ ] **Сохранить полное дерево на сервере** (в in-memory хранилище)
- [ ] Вернуть tree_id и **только метаданные узлов** (не полное дерево)
- [ ] Опциональные параметры:
  - `node_types` - фильтр по типам узлов (только FunctionDef, ClassDef, etc.)
  - `max_depth` - максимальная глубина для возврата
  - `include_children` - включать ли информацию о детях
- [ ] Формат ответа:
  ```json
  {
    "tree_id": "uuid-123",
    "file_path": "/path/to/file.py",
    "nodes": [
      {
        "node_id": "node-1",
        "type": "FunctionDef",
        "name": "main",
        "start_line": 10,
        "end_line": 15,
        "children_count": 5,
        "children": ["node-2", "node-3"]  // только ID детей, не полные узлы
      }
    ],
    "total_nodes": 42,
    "returned_nodes": 10
  }
  ```
- [ ] **Оптимизация**: передавать только нужные узлы, не все дерево

#### 2.2. Команда `cst_modify_tree`
- [ ] Создать `code_analysis/commands/cst_modify_tree_command.py`
- [ ] Реализовать операции:
  - `replace` - заменить узел по node_id
  - `insert` - вставить узел (before/after parent)
  - `remove` - удалить узел
- [ ] **Атомарность операций**:
  - Валидация всех операций перед применением
  - Проверка существования всех node_id
  - Проверка синтаксиса всех новых фрагментов кода
  - Либо все операции применяются, либо ничего (rollback)
- [ ] Поддержка batch operations (все операции в одном запросе атомарны)
- [ ] Формат запроса:
  ```json
  {
    "tree_id": "uuid-123",
    "operations": [
      {
        "action": "replace",
        "node_id": "node-1",
        "code": "def main():\n    pass"
      }
    ]
  }
  ```
- [ ] Обработка ошибок:
  - Если любая операция невалидна → откат всех изменений
  - Возврат детальной информации об ошибке

#### 2.3. Команда `cst_save_tree`
- [ ] Создать `code_analysis/commands/cst_save_tree_command.py`
- [ ] Реализовать **атомарное сохранение** дерева в файл
- [ ] **Последовательность атомарной записи**:
  1. Валидация всего файла через `compile()` (до любых изменений)
  2. Создание backup через `BackupManager` (если файл существует)
  3. Генерация исходного кода из CST дерева
  4. Запись в временный файл (`tempfile.NamedTemporaryFile`)
  5. Валидация временного файла (compile, linter, type checker, docstrings)
  6. Начало транзакции базы данных (`db.begin_transaction()`)
  7. Атомарная замена файла через `os.replace()` или `Path.replace()`
  8. Обновление базы данных (`update_file_data_atomic`)
  9. Коммит транзакции (`db.commit_transaction()`)
  10. Git commit (если требуется и `commit_message` предоставлен)
- [ ] **Rollback при ошибках**:
  - При ошибке на любом шаге → откат транзакции БД
  - Восстановление файла из backup (если был создан)
  - Удаление временного файла
  - Возврат детальной информации об ошибке
- [ ] Формат запроса:
  ```json
  {
    "tree_id": "uuid-123",
    "file_path": "/path/to/file.py",
    "validate": true,
    "backup": true,
    "commit_message": "Update main function"
  }
  ```
- [ ] **Гарантии атомарности**:
  - Файл либо полностью обновлен, либо остался без изменений
  - База данных либо полностью обновлена, либо откачена
  - Нет промежуточных состояний

#### 2.4. Команда `cst_find_node`
- [ ] Создать `code_analysis/commands/cst_find_node_command.py`
- [ ] Реализовать поиск по дереву на сервере (tree_id)
- [ ] **Два режима поиска**:
  1. **Простой поиск**:
     - По типу (FunctionDef, ClassDef, etc.)
     - По имени (name, qualname)
     - По позиции (line range)
  2. **XPath-подобный поиск** (используя CSTQuery):
     - Поддержка CSTQuery синтаксиса (уже реализован в `cst_query/`)
     - Примеры: `class[name="MyClass"]`, `function[name="f"] smallstmt[type="Return"]:first`
     - Все возможности CSTQuery (combinators, predicates, pseudos)
- [ ] Формат запроса:
  ```json
  {
    "tree_id": "uuid-123",
    "search_type": "xpath",  // "simple" или "xpath"
    "query": "class[name=\"MyClass\"]",  // для xpath
    // или для simple:
    "node_type": "FunctionDef",
    "name": "main"
  }
  ```
- [ ] Вернуть node_id и метаданные найденных узлов
- [ ] **Преимущество**: поиск по дереву на сервере, не нужно передавать все дерево

#### 2.5. Команда `cst_get_node_info` (опционально)
- [ ] Создать `code_analysis/commands/cst_get_node_info_command.py`
- [ ] Реализовать получение информации о узле из дерева на сервере
- [ ] Получение информации:
  - Дети узла (только ID и метаданные, не полные узлы)
  - Родитель узла (ID и метаданные)
  - Соседи узла (ID и метаданные)
  - Метаданные (тип, имя, позиция, код)
- [ ] Опциональные параметры:
  - `include_code` - включать ли исходный код узла
  - `include_children` - включать ли полную информацию о детях
  - `max_children` - ограничение количества детей

---

### Phase 3: Migration & Cleanup

#### 3.1. Удалить старые команды
- [ ] Удалить `code_analysis/commands/cst_compose_module_command.py`
- [ ] Удалить `code_analysis/commands/list_cst_blocks_command.py`
- [ ] Удалить `code_analysis/commands/query_cst_command.py`
  - **Заменяется на `cst_find_node` с XPath-подобным поиском**
  - CSTQuery синтаксис интегрирован в `cst_find_node`
  - Код `cst_query/` остается, используется внутри `cst_find_node`

#### 3.2. Обновить регистрацию команд
- [ ] Обновить `code_analysis/hooks.py`
  - Удалить регистрацию старых команд
  - Добавить регистрацию новых команд

#### 3.3. Удалить/рефакторить старый код
- [ ] Рефакторить `code_analysis/core/cst_module/`
  - Оставить только утилиты, которые используются новыми командами
  - Удалить неиспользуемый код
  - Возможно, переименовать в `cst_tree/` для ясности

#### 3.4. Обновить документацию
- [ ] Обновить `docs/CST_TOOLS.md` - новый workflow
- [ ] Обновить `docs/CST_QUERY.md` - если query_cst остается
- [ ] Создать `docs/CST_TREE_WORKFLOW.md` - новый workflow с примерами
- [ ] Удалить устаревшую документацию

---

### Phase 4: Testing & Validation

#### 4.1. Unit тесты
- [ ] Тесты для `cst_load_file`
- [ ] Тесты для `cst_modify_tree`
- [ ] Тесты для `cst_save_tree`
- [ ] Тесты для tree_id генерации
- [ ] Тесты для операций модификации

#### 4.2. Integration тесты
- [ ] Полный workflow: load → modify → save
- [ ] Batch operations (атомарность)
- [ ] Валидация и ошибки
- [ ] Backup и git commit
- [ ] **Тесты атомарности**:
  - Тест: ошибка в одной операции → все операции откатываются
  - Тест: ошибка при сохранении → файл не изменен, backup восстановлен
  - Тест: ошибка в БД → файл откатан, транзакция откачена
  - Тест: успешное сохранение → все изменения применены атомарно

#### 4.3. Миграция существующих тестов
- [ ] Обновить тесты, которые используют старые команды
- [ ] Удалить тесты для удаленных команд

---

### Phase 5: Cleanup & Optimization

#### 5.1. Удалить неиспользуемый код
- [ ] Удалить старые модели (`Selector`, `ReplaceOp`, `InsertOp`, `CreateOp` если не используются)
- [ ] Удалить старые патчеры, если не используются
- [ ] Очистить импорты

#### 5.2. Оптимизация
- [ ] Оптимизировать генерацию node_id
- [ ] Оптимизировать поиск узлов в дереве
- [ ] Оптимизировать применение операций

#### 5.3. Финальная проверка
- [ ] Проверить все команды работают
- [ ] Проверить документацию актуальна
- [ ] Проверить тесты проходят
- [ ] Проверить нет устаревших импортов

---

## 📋 Detailed Task Breakdown

### Task 1: Core Infrastructure (Phase 1)

**Файлы для создания**:
- `code_analysis/core/cst_tree/__init__.py`
- `code_analysis/core/cst_tree/models.py`
- `code_analysis/core/cst_tree/tree_builder.py`
- `code_analysis/core/cst_tree/tree_modifier.py`
- `code_analysis/core/cst_tree/tree_saver.py`
- `code_analysis/core/cst_tree/tree_finder.py` (XPath-поиск)
- `code_analysis/core/cst_tree/tree_metadata.py` (метаданные узлов)

**Ключевые функции**:

1. **`load_file_to_tree(file_path: str) -> CSTTree`**
   - Читает файл с диска
   - Парсит в CST дерево
   - Генерация node_id для всех узлов
   - **Сохранение полного дерева в in-memory хранилище с tree_id**
   - Возвращает tree_id (дерево остается на сервере)

2. **`modify_tree(tree_id: str, operations: list[TreeOperation]) -> CSTTree`**
   - **Атомарность**: валидация всех операций перед применением
   - **Процесс**:
     1. Загрузить дерево по tree_id
     2. Валидировать все операции (проверить node_id, синтаксис кода)
     3. Если все валидно → применить все операции
     4. Если ошибка → вернуть исходное дерево без изменений
   - **Rollback**: откат всех изменений при ошибке любой операции
   - Возвращает новое дерево (или исходное при ошибке)

3. **`save_tree_to_file(tree_id: str, file_path: str, **options) -> str`**
   - **Атомарность**: все шаги в транзакции
   - **Процесс**:
     1. Валидация всего файла через `compile()` (до изменений)
     2. Создание backup через `BackupManager` (если файл существует)
     3. Генерация исходного кода из CST дерева
     4. Запись в временный файл (`tempfile.NamedTemporaryFile`)
     5. Валидация временного файла (compile, linter, type checker)
     6. Начало транзакции БД (`db.begin_transaction()`)
     7. Атомарная замена: `os.replace(temp_file, target_file)`
     8. Обновление БД (`update_file_data_atomic`)
     9. Коммит транзакции (`db.commit_transaction()`)
     10. Git commit (если требуется)
   - **Rollback при ошибке**:
     - Откат транзакции БД (`db.rollback_transaction()`)
     - Восстановление файла из backup
     - Удаление временного файла
   - Возвращает путь к сохраненному файлу

---

### Task 2: New Commands (Phase 2)

**Файлы для создания**:
- `code_analysis/commands/cst_load_file_command.py`
- `code_analysis/commands/cst_modify_tree_command.py`
- `code_analysis/commands/cst_save_tree_command.py`
- `code_analysis/commands/cst_find_node_command.py` (с XPath-подобным поиском)
- `code_analysis/commands/cst_get_node_info_command.py` (опционально)

**Ключевые требования**:
- Простой и понятный API
- Хорошие сообщения об ошибках
- Подробная документация в metadata()
- **Атомарность операций** (либо все, либо ничего)
- **Детальная обработка ошибок** с rollback

---

### Task 3: Migration (Phase 3)

**Файлы для удаления**:
- `code_analysis/commands/cst_compose_module_command.py`
- `code_analysis/commands/list_cst_blocks_command.py`
- `code_analysis/commands/query_cst_command.py` (или упростить)

**Файлы для обновления**:
- `code_analysis/hooks.py`
- `docs/CST_TOOLS.md`
- `docs/CST_QUERY.md`

---

## 🎯 Success Criteria

1. ✅ Новые команды работают и проще в использовании
2. ✅ Старые команды удалены
3. ✅ Документация обновлена
4. ✅ Тесты проходят (включая тесты атомарности)
5. ✅ Нет устаревшего кода
6. ✅ Workflow упрощен: load → modify → save
7. ✅ **Все операции записи атомарны** (либо все, либо ничего)
8. ✅ **Rollback работает корректно** при любых ошибках
9. ✅ **Backup и восстановление** работают надежно

---

## 🔒 Atomicity Requirements

### Принципы атомарности:

1. **`cst_modify_tree` - атомарность операций**:
   - Все операции в одном запросе валидируются перед применением
   - Если любая операция невалидна → все операции откатываются
   - Дерево остается в исходном состоянии при ошибке

2. **`cst_save_tree` - атомарность записи**:
   - Валидация → Backup → Temp file → Atomic replace → DB update → Commit
   - При ошибке на любом шаге → полный rollback
   - Файл либо полностью обновлен, либо без изменений
   - База данных либо полностью обновлена, либо откачена

3. **Использование транзакций**:
   - Все операции с БД в транзакции
   - `db.begin_transaction()` → операции → `db.commit_transaction()`
   - При ошибке → `db.rollback_transaction()`

4. **Атомарная замена файла**:
   - Использовать `os.replace()` или `Path.replace()` вместо `shutil.move()`
   - Гарантирует атомарность на уровне файловой системы

5. **Backup и восстановление**:
   - Backup создается ДО любых изменений
   - При ошибке → автоматическое восстановление из backup
   - Backup удаляется только после успешного завершения

---

## 📝 Notes

- **НЕ нужна обратная совместимость** - проект еще не использовался
- **НЕ нужны fallback механизмы** - полный рефакторинг
- **Приоритет**: простота использования > гибкость
- **Подход**: императивный (работа с узлами) > декларативный (селекторы)
- **КРИТИЧНО**: Все операции записи должны быть атомарными (либо все, либо ничего)

## 🚀 Оптимизации передачи данных

### Принцип: Дерево на сервере, метаданные клиенту

1. **Полное CST дерево хранится на сервере**:
   - In-memory хранилище с tree_id
   - Клиент не получает полное дерево
   - Экономия трафика и памяти

2. **Клиент получает только метаданные узлов**:
   - node_id, type, name, position
   - Опционально: children_count, parent_id
   - По запросу: код узла, полная информация о детях

3. **XPath-подобный поиск на сервере**:
   - Поиск выполняется по дереву на сервере
   - Возвращаются только найденные узлы (с метаданными)
   - Не нужно передавать все дерево для поиска

4. **Преимущества**:
   - Меньше трафика (только нужные узлы)
   - Быстрее поиск (на сервере, по полному дереву)
   - Меньше памяти на клиенте
   - Масштабируемость (большие файлы не проблема)

---

## 🔗 Related Documents

- `docs/CST_TOOL_SIMPLIFICATION_ANALYSIS.md` - Анализ сложности текущего подхода
- `docs/CST_TOOLS.md` - Текущая документация (будет обновлена)
- `docs/CST_QUERY.md` - Документация CSTQuery (используется в `cst_find_node` для XPath-поиска)
- `code_analysis/cst_query/` - Существующая реализация CSTQuery (используется в `tree_finder.py`)
  - `cst_query/parser.py` - Lark parser для грамматики селекторов
  - `cst_query/executor.py` - Executor для поиска узлов
  - `cst_query/ast.py` - AST модели для CSTQuery

## 📚 Использование генератора грамматик (Lark)

### Текущее использование в проекте

**CSTQuery парсер** (`code_analysis/cst_query/parser.py`):
- Использует **Lark** для парсинга грамматики селекторов
- Грамматика определена в строке `_GRAMMAR` (EBNF формат)
- Парсер создается один раз: `_parser = Lark(_GRAMMAR, parser="lalr", start="start")`
- Transformer `_ToAst` преобразует Lark дерево в CSTQuery AST

**Преимущества подхода**:
- ✅ Декларативная грамматика (легко расширять)
- ✅ Автоматическая генерация парсера
- ✅ Хорошая производительность (LALR парсер)
- ✅ Четкие сообщения об ошибках

**Применение в новом API**:
- Использовать существующий Lark parser из `cst_query/parser.py`
- Не нужно создавать новый парсер - переиспользовать существующий
- Адаптировать executor для работы с CST деревом вместо исходного кода

## 🔍 XPath-подобный поиск: Детали реализации

### Интеграция существующего CSTQuery

**Существующий код** (уже используется в проекте):
- `code_analysis/cst_query/` - уже реализован CSTQuery парсер и executor
- **Использует Lark** для парсинга грамматики селекторов
- `cst_query/parser.py` - Lark parser с грамматикой для CSTQuery
- `cst_query/executor.py` - executor для поиска узлов
- `cst_query.query_source(source, selector)` - функция для поиска узлов

**Преимущества использования существующего кода**:
- ✅ Уже протестирован и работает
- ✅ Использует проверенный генератор грамматик (Lark)
- ✅ Не нужно переписывать парсер - **уже есть**
- ✅ Не нужно переписывать executor - **только добавить `query_tree()`**
- ✅ Поддерживает все возможности CSTQuery (combinators, predicates, pseudos)
- ✅ Минимальные изменения - просто адаптация существующего кода

**Использование в новом API**:
- `tree_finder.py` использует **новую функцию `query_tree()`** для XPath-поиска
- **Простая адаптация**: добавить `query_tree(module: cst.Module, selector: str)` в `executor.py`
  - Взять логику из `query_source()` (строки 88-112)
  - Убрать `cst.parse_module(source)` - дерево уже есть
  - Остальная логика остается той же
- **Важно**: поиск выполняется по полному дереву на сервере (не по исходному коду)
- Возвращаются только метаданные найденных узлов

**Примеры XPath-запросов**:
```python
# Найти класс по имени
"class[name=\"MyClass\"]"

# Найти метод по квалифицированному имени
"method[qualname=\"MyClass.my_method\"]"

# Найти все return statements
"smallstmt[type=\"Return\"]"

# Найти первый return в функции
"function[name=\"f\"] smallstmt[type=\"Return\"]:first"

# Найти второй return (0-based)
"smallstmt[type=\"Return\"]:nth(1)"
```

**Преимущества**:
- Использует существующий проверенный код CSTQuery
- Не нужно переписывать парсер
- Поддерживает все возможности CSTQuery (combinators, predicates, pseudos)
- Поиск на сервере по полному дереву (быстро и эффективно)
