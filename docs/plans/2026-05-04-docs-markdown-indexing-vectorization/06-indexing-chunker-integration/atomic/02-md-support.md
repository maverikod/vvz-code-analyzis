# Atomic step: md support

Parent step: 06-indexing-chunker-integration

Source: chunker file API

Goal: verify whether the existing chunker file API accepts `.md` input directly.

Output: chunker-file-api-contract.md

Rule: if unsupported, document the limitation and create a follow-up task for the chunker owner.
