# Runtime dependencies (mirrors pyproject.toml [project.dependencies], including PostgreSQL).
# Install: pip install -r requirements.txt
#
python-dotenv>=1.0
pyyaml>=6.0
click>=8.0
mcp>=1.0.0
pydantic>=2.1.0
svo-client>=3.0.0
lark>=1.1.0
libcst>=1.1.0
embed-client>=3.1.7.24
mcp-proxy-adapter>=8.10.6
queuemgr>=1.0.17

# PostgreSQL driver (основная СУБД в конфиге)
psycopg[binary]>=3.1

# Local package (code_analysis, casmgr CLI)
-e .
