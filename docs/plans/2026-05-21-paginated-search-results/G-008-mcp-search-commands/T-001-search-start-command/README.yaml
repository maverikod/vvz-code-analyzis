step_id: T-001
parent_global_step: G-008
name: search_start command
description: "Implement the search_start MCP command. Accepts search parameters (query,
  sources, filters, page_size). Creates a SearchJob via the infrastructure layer,
  runs foreground paging until two page files are written, then returns {job_id, page_url}
  to the caller. Background generation continues after return. Registers command metadata,
  parameter schema, and error codes (JOB_START_FAILED, INVALID_PARAMS).\n"
concepts:
- C-001
- C-020
- C-022
inputs:
- name: search_job_infrastructure
  type: module
  description: SearchJob creation and directory provisioning from G-001-si.
- name: foreground_paging_handoff
  type: protocol
  description: Two-page foreground barrier from G-003/T-006.
outputs:
- name: search_start_command
  type: mcp_command
  description: Registered search_start command module with schema and handler.
atomic_steps:
- A-001
status: draft
