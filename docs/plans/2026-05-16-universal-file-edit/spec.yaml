meta:
  plan_name: universal_file_edit
  source_spec: source_spec.md
  status: draft
concepts:
- concept_id: C-001
  name: EditSession
  definition: In-memory record that tracks one active editing session for one file.
    Identified by a session_id UUID. Owns the draft file path and the in-memory tree
    reference.
  properties:
  - identified by session_id (UUID)
  - stores file_path, draft_path, format (tree or text), tree_id
  - stored in a module-level registry in the server process
  - not persisted across server restarts
  source_ranges:
  - start: 107
    end: 121
- concept_id: C-002
  name: DraftFile
  definition: On-disk working copy of the file being edited. For tree formats persists
    permanently alongside the original. For text formats is temporary and deleted
    on close.
  properties:
  - path is <file>.draft
  - tree formats: contains serialised tree; persists after close
  - text formats: byte-for-byte copy of original; deleted on close
  - created by universal_file_open
  source_ranges:
  - start: 35
    end: 50
- concept_id: C-003
  name: WriteLockfile
  definition: On-disk lockfile that records the server PID of the process that initiated
    a write preview. Its presence signals that a diff has been shown and the next
    universal_file_write call should commit.
  properties:
  - path is <file>.write
  - contains the server process PID as text
  - created on first universal_file_write call
  - deleted on successful commit or on universal_file_open
  - PID mismatch means stale lock from a dead process; treated as first call
  source_ranges:
  - start: 52
    end: 58
- concept_id: C-004
  name: OpenCommand
  definition: MCP command that starts an editing session. Cleans up stale artefacts,
    creates the draft file, creates an initial backup when the file has no history,
    and returns the session_id.
  properties:
  - deletes stale <file>.write and <file>.draft on entry
  - creates <file>.draft by parsing the original file
  - creates initial backup if file has no backup history
  - returns session_id and list of available operations for the format
  source_ranges:
  - start: 60
    end: 81
- concept_id: C-005
  name: EditCommand
  definition: MCP command that applies a batch of mutation operations to <file>.draft.
    The original file is never touched.
  properties:
  - accepts insert, delete, replace operations
  - tree formats: validates batch then applies sequentially; writes draft after each
      op
  - text formats: sorts by start_line descending then applies; writes draft once
  - tree batch rejection is atomic on NESTED_BATCH_FORBIDDEN
  - original file is never modified
  source_ranges:
  - start: 83
    end: 133
- concept_id: C-006
  name: EditOperation
  definition: Single mutation instruction inside an edit batch. One of insert, delete,
    or replace. Address space depends on the file format.
  properties:
  - type is one of insert, delete, replace
  - tree format address is (parent_node_id, position_among_siblings)
  - text format address is (start_line, end_line) 1-based inclusive
  - content field required for insert and replace; absent for delete
  source_ranges:
  - start: 89
    end: 103
- concept_id: C-007
  name: BatchValidation
  definition: Pre-application check applied to tree format batches only. Ensures no
    ancestor-descendant conflicts. Text format uses sort-then-apply without pre-validation.
  properties:
  - tree only: runs before any operation is applied
  - tree only: rejects batch atomically on ancestor-descendant pair
  - tree only: error code NESTED_BATCH_FORBIDDEN on violation
  - text only: sort by start_line descending before application
  - text only: no ancestor-descendant concept applies
  source_ranges:
  - start: 105
    end: 127
- concept_id: C-008
  name: WriteCommand
  definition: Two-phase MCP command. First call shows a diff and creates the lockfile.
    Second call (lockfile present, PID matches) commits the draft to the original
    file with backup.
  properties:
  - first call generates code, computes diff, writes lockfile, returns diff
  - second call does backup then rename draft to original then delete lockfile then
    git commit
  - PID mismatch on second call is treated as first call (stale lock)
  - write failure after backup restores original from backup
  source_ranges:
  - start: 135
    end: 183
- concept_id: C-009
  name: CloseCommand
  definition: MCP command that ends an editing session. Behaviour differs by format.
  properties:
  - text formats: deletes <file>.draft and <file>.write, releases session
  - tree formats: <file>.draft is never deleted
  - tree formats: if checksums match leaves draft as is, releases session
  - tree formats: if checksums differ rebuilds draft from original without backup
  - returns draft_rebuilt flag when rebuild occurred
  source_ranges:
  - start: 185
    end: 211
- concept_id: C-010
  name: BackupPair
  definition: Atomic backup of the original file and its draft created together as
    one history entry. Count of backup pairs equals count of successful write commits.
  properties:
  - created only on successful universal_file_write commit
  - contains both <file> and <file>.draft at commit time
  - one entry in backup history per commit
  - initial backup created by universal_file_open when file has no history
  source_ranges:
  - start: 70
    end: 74
  - start: 157
    end: 167
- concept_id: C-011
  name: HandlerReuse
  definition: The four new commands reuse the handler infrastructure of universal_file_preview
    without creating a new package.
  properties:
  - imports HandlerDispatcher from universal_file_preview/dispatcher.py
  - imports FileHandler ABC from universal_file_preview/base_handler.py
  - imports resolve_session from universal_file_preview/session.py
  - imports Node and NodeKind from universal_file_preview/models.py
  - imports concrete handlers from universal_file_preview/handlers/
  source_ranges:
  - start: 213
    end: 226
- concept_id: C-012
  name: RegistrationCleanup
  definition: Removal of the legacy editing command zoo from hooks.py registration.
    Code is not deleted; only registration is removed.
  properties:
  - removes universal_file_save, universal_file_replace, universal_file_read, universal_file_delete
  - removes all cst_* and json_* tree commands
  - removes write_project_text_lines, replace_file_lines, compose_cst_module, query_cst,
    list_cst_blocks, read_project_text_file
  - get_file_lines remains registered as fallback for broken files
  - universal_file_preview remains registered unchanged
  source_ranges:
  - start: 14
    end: 32
relations:
- from_concept: C-004
  to_concept: C-001
  type: produces
- from_concept: C-005
  to_concept: C-001
  type: consumes
- from_concept: C-008
  to_concept: C-001
  type: consumes
- from_concept: C-009
  to_concept: C-001
  type: consumes
- from_concept: C-004
  to_concept: C-002
  type: produces
- from_concept: C-005
  to_concept: C-002
  type: uses
- from_concept: C-008
  to_concept: C-002
  type: uses
- from_concept: C-009
  to_concept: C-002
  type: uses
- from_concept: C-008
  to_concept: C-003
  type: produces
- from_concept: C-009
  to_concept: C-003
  type: uses
- from_concept: C-004
  to_concept: C-003
  type: uses
- from_concept: C-005
  to_concept: C-006
  type: consumes
- from_concept: C-005
  to_concept: C-007
  type: uses
- from_concept: C-007
  to_concept: C-006
  type: consumes
- from_concept: C-008
  to_concept: C-010
  type: produces
- from_concept: C-004
  to_concept: C-010
  type: produces
- from_concept: C-004
  to_concept: C-011
  type: uses
- from_concept: C-005
  to_concept: C-011
  type: uses
- from_concept: C-008
  to_concept: C-011
  type: uses
- from_concept: C-009
  to_concept: C-011
  type: uses
- from_concept: C-012
  to_concept: C-004
  type: depends_on
- from_concept: C-012
  to_concept: C-005
  type: depends_on
- from_concept: C-012
  to_concept: C-008
  type: depends_on
- from_concept: C-012
  to_concept: C-009
  type: depends_on
