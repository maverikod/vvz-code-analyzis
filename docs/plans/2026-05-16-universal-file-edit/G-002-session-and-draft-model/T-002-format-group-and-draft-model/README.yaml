step_id: T-002
parent_global_step: G-002
name: Define FormatGroup enum and DraftFile and WriteLockfile models
description: Define the FormatGroup enum (C-002) with values sidecar, tree-temp,
  text. Define resolve_format_group(ext) that maps file extension to FormatGroup.
  Define draft_path_for(abs_path, format_group) returning the DraftFile path
  (C-003): for sidecar returns <file>.cst_sidecar, for tree-temp and text
  returns <file>.draft. Define lockfile_path_for(abs_path) returning <file>.write.
  Define read_lockfile_pid(abs_path) and write_lockfile_pid(abs_path, pid) for
  WriteLockfile (C-004) access. All functions are pure; no I/O side effects.
concepts:
- C-002
- C-003
- C-004
inputs:
- name: nothing
  type: new module
  description: No existing file; this T-step creates the models module from scratch.
outputs:
- name: models_module
  type: new source file
  description: Module containing FormatGroup enum, resolve_format_group,
    draft_path_for, lockfile_path_for, read_lockfile_pid, write_lockfile_pid.
atomic_steps: []
status: draft