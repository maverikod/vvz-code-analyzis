step_id: A-003
parent_tactical_step: T-003
name: project_cross_search pagination schema extension module
target_file: code_analysis/commands/project_cross_search_pagination_schema.py
operation: create_file
priority: 3
depends_on: []
concepts:
  - C-021
  - C-023
  - C-024
prompt: |
  Operation: create_file
  Target: code_analysis/commands/project_cross_search_pagination_schema.py

  MRS (C-024 OptionalPaginationParameters):
  - May include paginated, job_id, include_job_id, block position number.
  - Must not be required for existing non-paginated callers.

  Precondition: code_analysis/commands/search_session_schema.py exports merge_pagination_schema.

  Do not modify project_cross_search_command.py (monolith over 400 lines).

  Implement:
  - def get_project_cross_search_schema_with_pagination() -> dict[str, Any]
    Import get_project_cross_search_schema from code_analysis.commands.project_cross_search_command,
    return merge_pagination_schema(base).
  - def document_cross_search_pagination_metadata() -> dict[str, str]
    Short descriptions for paginated, job_id, include_job_id, block_position.

  Constraints:
  - Under 80 lines.
  - Author header.
  - Schema-only; merged evidence paging execute wiring is search_paginated_cross.py.

verification:
  type: pytest
  target: tests/unit/test_project_cross_search_pagination_schema.py
  expected: >
    Schema includes optional pagination fields; project_cross_search base required parameters
    unchanged; metadata helper returns four documented keys.
status: ready_for_review
