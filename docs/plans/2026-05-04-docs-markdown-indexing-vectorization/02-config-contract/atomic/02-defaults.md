# Atomic step: defaults

Parent step: 02-config-contract

Source file or area:
code_analysis config defaults

Goal:
Document safe defaults for docs indexing: `enabled=false`, `vectorize=false`, `roots=["docs"]`, include Markdown patterns, and default exclusions.

Output:
docs/plans/2026-05-04-docs-markdown-indexing-vectorization/02-config-contract/config-contract.md

Rules:
Docs indexing and docs vectorization must remain disabled by default.
