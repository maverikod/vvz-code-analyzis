# Atomic step: config entry

Parent step: 05-watcher-integration

Source: watcher config entry point

Goal: find where watcher loads server config and where docs_indexing can be read.

Output: watcher integration notes.
